# Contracts — Foundry

Week 1: `AccrualStream` start → cancel → settle elapsed on Monad testnet (chain 10143 / 143 mainnet).

Do not settle every second. The UI ticks from `ratePerSecond` and `startedAt`.

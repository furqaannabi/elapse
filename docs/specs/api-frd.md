# Platform API (`api/` — REST + Postgres) — FRD

Status: **FR-API-139/140 and the FR-API-030/040 amendments (merchant stops; no hosted checkout) Signed 2026-09-17 (Furqaan)** · **FR-API-137/138 (merchant-started metering for the subscriber) Signed 2026-09-16 (Furqaan)** · **FR-API-049/127 and the FR-API-033/071 amendments (merchant-started metering) Signed 2026-09-14 (Furqaan)** · **Signed 2026-09-05 (William); FR-API-120/125 and the FR-API-032 amendment signed 2026-09-07 (William); FR-API-034 signed 2026-09-07 (William); FR-API-035/036 signed 2026-09-07 (William); FR-API-032/034 live-on-testnet amendment signed 2026-09-07 (William); FR-API-065/104/113 and the FR-API-103 amendment signed 2026-09-07 (William); FR-API-121/123 amendments signed 2026-09-07 (William); FR-API-044..047 relayed pause/resume signed 2026-09-07 (William); FR-API-048 and the FR-API-034 AUSD amendment signed 2026-09-07 (William); FR-API-126 signed 2026-09-07 (William); FR-API-121 `checkout_session` amendment signed 2026-09-07 (William); FR-API-126 `restarted_as` amendment signed 2026-09-07 (William); FR-API-136 signed 2026-09-09 (William)** · Surface: Platform (merchant-facing REST, internal ingest, dashboard backend) · Sources: detailed doc §2, §3 Objects, §4.2 frozen SDK surface, §4.4, §5.1–5.3, §6 item 7 & 9, §8, §9 Architecture, §12 Week 2/4, §15; `api/README.md`; `.env.example`; checkout FRD (Data block, BR-CHK-005); dashboard FRD outline; design brief §3.

## Problem

Merchants must integrate "in an afternoon" with the nouns they already know from Stripe (doc §2, §3). The API is the only thing the SDK, the hosted Checkout, the dashboard and the docs talk to. It owns the truth about Merchants, Products, Customers, Checkout sessions, Subscriptions, Invoices/Settlements, Webhook endpoints, Events and Deliveries; it turns indexed chain events into merchant Events; and it must serve exactly the frozen §4.2 surface — "anything not listed does not exist" (§15).

## User stories

1. As a Merchant engineer, I want `products.create` and `checkout.sessions.create` with my secret key and get back `session.url`, so that the quickstart takes under 10 minutes (§6).
2. As a Merchant engineer, I want `GET /v1/subscriptions/:id` with `rate` and `started_at`, so that I can compute a live meter locally without per-second webhooks (§5.1).
3. As a Merchant, I want separate test and live keys, hashed at rest and shown once, so that a leaked database does not leak my keys (§9).
4. As the hosted Checkout, I want to load a session with only a publishable key and a session id, so that the secret key never reaches the browser (BR-CHK-005).
5. As the indexer, I want an ingest endpoint that accepts the same chain event twice without side effects, so that Envio preload double-fires are harmless (§15).
6. As a demo presenter, I want test clocks in test mode, so that the video shows an hour of accrual in seconds (§6 item 9).
7. As a judge, I want a published OpenAPI document that matches the SDK, so that the docs are not a lie (§6 item 7, §15).

## Functional requirements

### Auth, keys, modes (doc §2 "publishable key + secret key, Stripe-style", §9 "API keys hashed at rest", §8)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-001 | Merchant endpoints authenticate with `Authorization: Bearer sk_(test|live)_…`. Missing/invalid key → `401 authentication_error`. The key's mode (test/live) scopes every query. | Test: live key cannot read a test Product (`404`). |
| FR-API-002 | Keys: `pk_test_`/`pk_live_` (one per Merchant per mode, visible), `sk_test_`/`sk_live_` (many, named). Secret keys stored as SHA-256 hash + last 4 chars; plaintext returned exactly once at creation. | DB has no plaintext; `GET /v1/api_keys` returns `sk_test_…abcd`. |
| FR-API-003 | `POST /v1/api_keys`, `GET /v1/api_keys`, `POST /v1/api_keys/:id/roll` (new key, old valid for the grace the Merchant chose — FR-API-105), `DELETE /v1/api_keys/:id` (revoke immediately). Dashboard-session authenticated, not sk_. | Revoked key → `401` on next request. |
| FR-API-004 | Publishable key authenticates only `GET /v1/checkout/sessions/:id` (public fields) and the Checkout's session-scoped actions (FR-API-032). **The hosted page itself sends no key** (William 2026-09-05, option a, the Stripe Checkout model): those same routes accept a request with no `Authorization` whose `Origin` is the checkout origin (`NEXT_PUBLIC_CHECKOUT_URL`), the session id being the capability; CORS is opened for that origin only. | pk_ on `POST /v1/products` → `401`. |
| FR-API-005 | Rate limit per key: `429 rate_limit_error` with `Retry-After`; ingest endpoint exempt. Limits in Undecided 6. | Burst test. |
| FR-API-006 | Audit log table: key create/roll/revoke, secret reveals, webhook endpoint changes, resends, test-clock advances — with actor, ip, at. Read-only in dashboard. | Row written per action; test asserts count. |

### Products (doc §3, §4.2 `products.create/retrieve/list`)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-010 | `POST /v1/products {name, rate_usd_per_second, allow_pause?, description?}` → `prod_…`, `currency: "ausd"`, `rate_per_second_wei` derived from token decimals, `active: true`. | Create at `"0.004"` with 6-dec token → `rate_per_second_wei: "4000"`. |
| FR-API-011 | `GET /v1/products/:id`, `GET /v1/products` (paginated), `POST /v1/products/:id` (name, description, allow_pause, `active:false` = archive). Rate is immutable after creation (doc §14: one product, one rate). | Changing rate → `400 invalid_request_error param=rate_usd_per_second`. |

### Customers (doc §3; §4.2 `customers.retrieve`)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-020 | Customers (`cus_…`) are created by the platform when a Subscriber completes Privy auth in Checkout: `{email?, passkey_id?, wallet_address, default_payment: "ausd"}`. One Customer per (Merchant, wallet_address). | Second session with same wallet reuses `cus_`. |
| FR-API-021 | `GET /v1/customers/:id`, `GET /v1/customers`. No create/update in the SDK surface (§4.2). | 200 / 404 tests. |

### Checkout sessions (doc §4.2 `checkout.sessions.create`, §7; checkout FRD Data block)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-030 | `POST /v1/checkout/sessions {product, success_url, cancel_url, customer?, max_duration_seconds?}` → `{id: cs_…, url: "<CHECKOUT_BASE>/c/cs_…", status: "open", expires_at: +24h, product, merchant: {name, logo_url}, max_duration_seconds?, max_escrow_usd?}`. When the merchant passes `max_duration_seconds` the checkout's cap step (checkout FR-CHK-003) is fixed to it; otherwise the subscriber picks. `max_escrow_usd = rate × max_duration_seconds`, exact decimal ([ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md)). Archived product → `400`; `max_duration_seconds` outside 60–2 592 000 → `400`. **Amended 2026-09-17 (awaiting sign-off, FR-API-140):** no `url` in the response once the hosted checkout is removed. | URL uses `NEXT_PUBLIC_CHECKOUT_URL`; `max_escrow_usd` exact for `0.004 × 3600 = 14.4`. |
| FR-API-031 | `GET /v1/checkout/sessions/:id` — with sk_: full object; with pk_: public projection `{id, status, merchant{name,logo_url,success_url,cancel_url}, product{id,name,rate_usd_per_second,allow_pause}, subscription?}` (exactly the checkout FRD `CheckoutSession` type). | Snapshot test of both projections. |
| FR-API-032 | Session-scoped Checkout actions (checkout-origin pass + session id; the two **binding** calls, `prepare` and `cancel/prepare`, also require the Privy identity token of FR-API-120 and take the wallet and email from it — the body's `wallet_address` and `email` are gone ([ADR 2026-09-07 identity token](../decisions/2026-09-07-privy-identity-token-on-prepare.md)); `start` and `cancel` stay proven by their signatures), [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md): `POST /v1/checkout/sessions/:id/prepare {max_duration_seconds}` (token wallet) creates the Customer and the Subscription (`incomplete`), computes `max_escrow` in token units, and returns the EIP-712 permit payload to sign `{domain, types, message: {owner, spender: factory, value: max_escrow, nonce, deadline: now + 10 min}}` plus `{max_escrow_usd, chain_id}`. `POST /v1/checkout/sessions/:id/start {signature}` verifies the signature recovers to the Customer's wallet, then the relayer submits one transaction: `permit` → `StreamFactory.createWithPermit(merchant, subscriber, token, rate, maxEscrow, permitSig)` which pulls the escrow and starts the stream (contracts FR-CON-015/016); stores `stream_address`, `pending_tx`; returns `202`. **Whenever the escrow token is `MockUSD`** (chain 10143, which until mainnet is both modes, [ADR 2026-09-07 testnet submission](../decisions/2026-09-07-submission-on-testnet-live-mode-mockusd.md)) the relayer first mints `max_escrow` of `MockUSD` to the subscriber's wallet if their balance is short, so a checkout never depends on a faucet (Undecided 4). The mint keys off the token, never off `livemode`. **Amended 2026-09-13** ([ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md)): the mint clause is deleted — MockUSD left the runtime, the escrow token is the chain's AUSD in both modes, and nothing is ever minted; a wallet short of `max_escrow` is refused by FR-API-034 in either mode. `active` arrives via ingest (BR-API-005). A second `start` on the same session → `409`. | Mocked viem; permit payload snapshot; wrong signer → `400`; second start → `409`. |
| FR-API-033 | Session states `open → complete` (**amended 2026-09-14:** on the funding event — `Deposited` for a `NoStart` create — not on `StreamStarted`, because with merchant-started metering the subscriber is finished at authorisation and the session must not be preparable again while the merchant is still getting ready) / `expired` (after `expires_at`, cron). A `complete` or `expired` session cannot be prepared again. | State tests; checkout FRD FR-CHK-010. |
| FR-API-034 | Balance check at `start` when the escrow token cannot be minted (William 2026-09-07, option 1; re-gated 2026-09-07 from `livemode` to the token, [ADR 2026-09-07 testnet submission](../decisions/2026-09-07-submission-on-testnet-live-mode-mockusd.md)): after the signature is verified and before anything is submitted, the API reads the subscriber's `balanceOf` of the escrow token and, if the token is AUSD and the balance is below `max_escrow`, answers `400 invalid_request_error code=insufficient_balance` with the message "This meter needs $14.40 to start. Your balance is $3.10." (both figures in USD, two decimals, no chain words). No transaction is sent and no relayer gas is spent. A `MockUSD` chain keeps its top-up (FR-API-032) in both modes and never refuses. **Amended 2026-09-07** ([ADR 2026-09-07 add funds](../decisions/2026-09-07-add-money-and-ausd-live-on-testnet.md)): on testnet 10143 the escrow token is now **mode-aware** — live mode escrows AUSD (`deployments/10143.json` `ausd`), test mode escrows MockUSD; minting stays keyed to MockUSD, so a live wallet can be short and is refused here, while a test wallet is topped up. Supersedes the "live mode on MockUSD" clause of the testnet ADR. **Amended 2026-09-13** ([ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md)): there is no mintable token any more — the escrow token is the chain's AUSD in both modes and the balance check runs unconditionally: short of `max_escrow` → the same `400 insufficient_balance`, no transaction, no relayer gas, no mint, regardless of mode. | Mocked chain: short AUSD balance → 400 with both amounts in the message and no `createWithPermit` call; exact balance → 202; `MockUSD` short in either mode → mint then 202. |
| FR-API-035 | Payout address gates Checkout ([ADR 2026-09-07 payout gate](../decisions/2026-09-07-payout-address-gates-checkout-and-live-keys.md)): `POST /v1/checkout/sessions` in either mode is refused with `400 invalid_request_error code=no_payout_address` ("Set a payout address in Settings before creating checkout links.") while `merchants.payout_address` is null, so a subscriber never reaches a meter the merchant cannot be paid for; `start`'s own guard (FR-API-032) stays as the last line. `GET /v1/dashboard/profile` (or the existing profile read) exposes `payout_address_set: boolean` for the banner. | Route test: no address → 400 with that message and no session row; set it → 201. |
| FR-API-036 | Live keys need a payout address ([ADR 2026-09-07 payout gate](../decisions/2026-09-07-payout-address-gates-checkout-and-live-keys.md)): `POST /v1/api_keys` for live mode and the live publishable key read are refused with `400 invalid_request_error code=no_payout_address` ("Set a payout address before going live.") while it is null. Test keys are unaffected; a live key that already exists keeps working. | Route test: live create → 400; test create → 201; set address → live create 201. |

### Subscriptions (doc §3 status machine, §4.2 `subscriptions.retrieve/cancel`, §5.1 polling)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-040 | Subscription object: `{id: sub_…, status: incomplete|active|paused|canceled, product, customer, rate_usd_per_second, started_at, paused_at, canceled_at, ended_reason: "canceled"|"cap_reached"|null, max_duration_seconds, max_escrow_usd, funded_usd, settled_usd, seconds_elapsed, stream_address, chain_id, checkout_session, manage_url}`. `manage_url` is the hosted session page `{checkout base}/c/{checkout_session}`, always a string including after cancel, present in reads and in every `subscription.*` event ([ADR 2026-09-09 manage_url](../decisions/2026-09-09-subscription-manage-url.md)). Status changes only from ingested chain events (FR-API-070). **Amended 2026-09-17 (awaiting sign-off, FR-API-140):** `manage_url` points to `/account`. | Schema test; `manage_url` on retrieve, list, cancel and on the `subscription.canceled` event body; `seconds_elapsed` computed as `min(now − started_at − paused, max_seconds)` for `active`. |
| FR-API-041 | `GET /v1/subscriptions/:id`, `GET /v1/subscriptions?status=&customer=&product=` with the FR-API-080 cursor. This route is now part of the frozen SDK surface as `subscriptions.list` (SDK FR-SDK-008, approved 2026-09-04), so it must appear in `openapi.json` as a public operation, not a dashboard-internal one. | Filters tested; operation present in `openapi.json`; SDK contract test (FR-API-084) covers it. |
| FR-API-042 | `POST /v1/subscriptions/:id/cancel` — Merchant-initiated (SDK `subscriptions.cancel`): the relayer, as the factory's keeper, submits `cancel()` (FR-CON-054, ADR 2026-09-05) and the API returns `202` with the unchanged Subscription plus `pending_tx`; the `canceled` status arrives only via ingest (BR-API-005). | Mocked chain; `202` with `pending_tx`; status still `active` until the `StreamCanceled` fixture is ingested. |
| FR-API-043 | ~~Pause/resume from the dashboard (`POST /v1/subscriptions/:id/pause|resume`) exist as dashboard-internal routes, hidden from OpenAPI/SDK (Undecided 3).~~ **Superseded 2026-09-07** ([ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md)): no merchant pause route exists (dashboard decision 7 already refused it on the UI); the subscriber routes are FR-API-044..047. | Not in `openapi.json`; `POST /v1/subscriptions/:id/pause` → `404`. |
| FR-API-044 | **Subscriber pause under the session** ([ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md)): `POST /v1/checkout/sessions/:id/pause/prepare` (checkout pass + identity token, a binding call as FR-API-032 cancel/prepare) returns `{message, deadline}` for the stream's `pauseDigest(relayNonce, now + 10 min)` (contracts FR-CON-018); `POST …/pause {signature, deadline}` submits `pauseFor` through the relayer, `202` with the unchanged Subscription plus `pending_tx`; `paused` arrives via ingest (BR-API-005). Requires the Subscription `active` and the Product `allow_pause`; else `400 invalid_state` / `400 pause_not_allowed`. | Mocked chain: `202`; `allow_pause: false` → `400 pause_not_allowed`; paused already → `400 invalid_state`; bad signature → `400`. |
| FR-API-045 | **Subscriber resume under the session**: `POST /v1/checkout/sessions/:id/resume/prepare` → `{message, deadline}` for `resumeDigest`; `POST …/resume {signature, deadline}` submits `resumeFor`, `202` + `pending_tx`; `active` arrives via ingest. Requires the Subscription `paused`. `allow_pause` is not re-checked: a meter paused while the flag was on may always resume. | `202`; active already → `400 invalid_state`. |
| FR-API-046 | **Account pause/resume**: `POST /v1/account/subscriptions/:id/pause/prepare`, `…/pause`, `…/resume/prepare`, `…/resume` mirror FR-API-044/045 with the identity token instead of the session pass, reusing the same service through the subscription's checkout session (as FR-API-123 cancel does); another wallet's subscription → `404`. FR-API-121 rows carry `product.allow_pause` so the account page knows whether to offer Pause. | `202` for the caller's own subscription; other wallet → `404`; `allow_pause` present in the account row. |
| FR-API-047 | **Pause/resume rate limit**: more than 10 pause-or-resume submissions per Subscription per hour (prepare calls do not count) → `429 rate_limit_error`, the FR-API-126 shape. Each submission is a relayer transaction and a `subscription.updated` delivery, so the count bounds gas and merchant traffic. Audit rows `subscription.pause` / `subscription.resume` (FR-API-105) with the subscription id. | 10 pass, the 11th → `429`; the window is per subscription, not per wallet. |
| FR-API-048 | **Wallet balance for the checkout** ([ADR 2026-09-07 add funds](../decisions/2026-09-07-add-money-and-ausd-live-on-testnet.md)): `GET /v1/checkout/sessions/:id/balance` (checkout pass + identity token, FR-API-120) reads `balanceOf` of the session's escrow token for the token's wallet and answers `{balance_usd, needs_funding, receive_address, token: "AUSD" \| "Test dollars", network: "Monad" \| "Monad testnet", chain_id}` — `needs_funding` is `!isMintable(token)`, so test mode always answers `false` and the page never shows Add funds there. **Amended 2026-09-13** ([ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md)): `needs_funding` is `true` in both modes (no token is mintable), `token` is always `"AUSD"`, and `network` names the chain the mode runs on — "Monad testnet" for both until a mainnet record exists, then "Monad" for live. Figures are USD decimal strings from base units, never floats. Rate-limited with the other checkout reads. | Mocked chain: live session → balance and `needs_funding: true`; test session → `needs_funding: false`; another subscriber's token → 403; no token → 401. |

### Invoices / Settlements (doc §3 Invoice, §5.1 `invoice.settled`; design brief §3.7)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-050 | Each ingested `Settled(seconds, amount, fee)` creates an Invoice `{id: in_…, subscription, period_start, period_end, seconds, amount_settled (decimal string, gross — the §5.3 field), gross, fee, net, currency: "ausd", tx_hash, log_index, status: "paid"}` where `gross == amount_settled`, `net = gross − fee` (dashboard decision 4b, FR-DSH-060). Cancel produces one Invoice for the final chunk. | Ingest fixture → one row; `gross == seconds × rate` exactly; `net + fee == gross`. |
| FR-API-051 | Reshaped 2026-09-04 by the cap end (contracts FR-CON-041): `StreamPaused reason=1` no longer exists, so no Invoice is created with `status: "failed"` from the chain. A cap-ended Subscription is recognised by `seconds_elapsed == max_duration_seconds` on the ingested `StreamCanceled` and is stored with `ended_reason: "cap_reached"` (vs `"canceled"`). A cap end also creates an Invoice with `status: "failed"`, `seconds: 0`, `amount_settled: "0"` to anchor `invoice.payment_failed`, which fires alongside `subscription.canceled` (decided 2026-09-04, William: a merchant must be able to tell "ran out" from "chose to stop" without reading a field). A subscriber cancel fires `subscription.canceled` alone. | Fixture: cap-ended cancel → Subscription `canceled` with `ended_reason: "cap_reached"`, one `paid` Invoice for the final chunk, one `failed` Invoice, and both Events; subscriber cancel → one Event. |
| FR-API-052 | `GET /v1/invoices?subscription=&customer=` (SDK `invoices.list`), `GET /v1/invoices/:id`. | Pagination test. |

### Webhook endpoints, events, deliveries (doc §3, §5.1–5.3; design brief §3.9–3.10)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-060 | `POST /v1/webhook_endpoints {url, events[]}` → `{id: wh_…, url, events, disabled:false, secret: "whsec_…"}`. The secret is returned once and stored encrypted, not hashed, because the worker must sign with it (Undecided 7). `events[]` must be a subset of the §5.1 catalog or `["*"]`. | Unknown type → `400 param=events`; `GET` never returns `secret`. |
| FR-API-061 | `GET/POST/DELETE /v1/webhook_endpoints/:id` (update url/events/disabled), `POST …/roll_secret` (new `whsec_` returned once; old kept until `previous_secret_expires_at`, worker FRD FR-WRK-040), `POST …/test` (enqueues a synthetic Event of the chosen type, worker delivers normally). | Roll returns new secret; row has both. |
| FR-API-062 | Live-mode endpoint URLs must be `https://` and not resolve to private/loopback ranges; test mode allows `http://` (ngrok, CLI forwarder). | Validation tests. |
| FR-API-063 | Events: `{id: evt_…, object: "event", type, created (unix s), livemode, data: {object}, pending_webhooks, request?}` exactly per §5.3; `GET /v1/events?type=`, `GET /v1/events/:id`. `pending_webhooks` = count of Deliveries not yet succeeded or exhausted. | Snapshot equals §5.3 example for `subscription.canceled`. |
| FR-API-136 | **Event context for the dashboard** (William, 2026-09-09; dashboard FR-DSH-093): `GET /v1/events` and `GET /v1/events/:id` add `context: {product_name, customer, customer_email, amount_settled?} \| null` **only when the request carries a dashboard session**; an `sk_` request keeps the FR-API-063 shape byte for byte and `openapi.json` does not list the field. Resolved in the same query from the payload's `product` and `customer` ids for subscription and checkout events, and through `subscription` for invoice events (`amount_settled` from the invoice); `customer` is the `cus_` id (build note 2026-09-09: added so the dashboard can name an email-less customer on invoice events, whose payload carries no customer id); `customer_email` is null when the Customer has none. `null` when the product or subscription no longer resolves. Scoped to the session's Merchant and mode like the row itself. | Session request carries `context`; the same list under a key does not; an invoice event resolves product and email through its subscription; a customer without email → `customer_email: null`; a deleted product → `context: null`; a second merchant's product name never appears. |
| FR-API-049 | **Merchant starts the meter** (2026-09-14). `POST /v1/subscriptions/:id/start` (secret key) submits `start()` through the relayer for a Subscription whose status is `incomplete` and whose stream is funded (contracts FR-CON-055), stores `pending_tx`, returns `202` with the Subscription. `active` arrives via ingest (BR-API-005). A Subscription already `active` → `409`; `canceled` or `paused` → `409`; one whose stream was never funded → `409 not_funded`. Idempotent on `Idempotency-Key`. | Mocked viem: `incomplete` + funded → 202 and one relayer submit; second call → 409; wrong merchant's subscription → 404. |
| FR-API-127 | **Authorised-but-unstarted expiry** (2026-09-14). A Subscription that has been `incomplete` with a funded stream for longer than `min(max_duration_seconds, 15 min)` is cancelled by the keeper (worker FR-WRK-075), refunding the subscriber in full (contracts FR-CON-056). The window bounds how long a merchant may hold a subscriber's escrow without starting. **Recommended default, confirm at signature:** the session's `max_duration_seconds` capped at 15 minutes. | A funded, unstarted Subscription older than the window is selected and cancelled; one inside the window is not; a started one is never selected. |
| FR-API-137 | **Start mode and refund-by time for the subscriber** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). The public checkout-session projection (FR-API-031) carries, on its `subscription`, `start_mode` and `start_by`: the unix time at which the FR-WRK-075 sweep will refund an unstarted session, `created + min(max_duration_seconds, UNSTARTED_WINDOW_S)`, computed by one exported function that the sweep also uses, and `null` once started or in `checkout` mode. The checkout's signed cancel (`cancel/prepare` and `cancel`, FR-API-032) accepts a funded, unstarted `merchant`-mode subscription, and a subscriber cancel submitted on one stamps `cancel_submitted_at`, so the sweep never sends a second cancel for the same stream. The merchant-facing Subscription object and the SDK types are unchanged. | Public session for a funded unstarted `merchant` row carries both fields, with `start_by` equal to the sweep's cutoff; an `active` row → `start_by: null`; a signed cancel on the held row → `202` and `cancel_submitted_at` set; the sweep then skips it. |
| FR-API-139 | **Refuse what the contract will refuse** ([ADR 2026-09-17](../decisions/2026-09-17-merchant-started-meters-only-merchant-stops.md)). For a `merchant`-mode subscription that is `active` or `paused`, the subscriber routes `checkout/sessions/:id/cancel/prepare`, `cancel`, `pause/prepare`, `pause`, `resume/prepare`, `resume` and the `/account` equivalents answer `409 merchant_controlled` "Only {merchant} can stop this meter." and nothing goes to the relayer. The public projection's subscription carries `subscriber_can_stop: boolean`. The merchant's `subscriptions.cancel` (FR-API-042) is unchanged. | Each route on a started merchant-mode row → `409 merchant_controlled` and zero relayer submissions; a held row → still accepted (FR-API-137); checkout mode unchanged; `subscriber_can_stop` true, false and true for held, started merchant, checkout. |
| FR-API-140 | **The API without a hosted checkout** ([ADR 2026-09-17](../decisions/2026-09-17-react-sdk-replaces-hosted-checkout.md)). (a) `POST /v1/checkout/sessions` no longer returns `url` (FR-API-030); `success_url` and `cancel_url` stay. (b) `manage_url` on the Subscription (FR-API-040) is `{DASHBOARD_ORIGIN}/account`. (c) Read-only public session routes (`GET /v1/checkout/sessions/:id` with a publishable key) allow any origin (CORS, no credentials); every mutating subscriber route stays same-origin to the Elapse popup. (d) The popup posts its result only to the origin of the session's `success_url`, and refuses to open for a session whose `success_url` is not `https` (http allowed for `localhost` in test mode). | Response of `checkout.sessions.create` has no `url`; `manage_url` equals the account URL; preflight from `https://shop.test` on the public GET succeeds and on `prepare` fails; popup origin rule covered in checkout FR-CHK-038. |
| FR-API-064 | Deliveries: `GET /v1/webhook_endpoints/:id/deliveries`, `GET /v1/deliveries/:id` (attempts with status code, response excerpt, duration, headers incl. `X-Elapse-Signature`), `POST /v1/deliveries/:id/resend` (worker FRD FR-WRK-031). | Resend creates a new attempt row. |
| FR-API-065 | Endpoint URL bounds ([ADR 2026-09-07 forms hardening](../decisions/2026-09-07-forms-hardening-logo-png-revoke-confirm.md)): `url` on create and update is `max(2048)`; an over-length URL → `400 param=url`. | Route test: 2049 chars → 400, 2048 → 201. |

### CLI sessions (CLI FRD FR-CLI-010–020; [ADR 2026-09-06](../decisions/2026-09-06-cli-transport-and-session.md))

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-130 | `POST /v1/cli/sessions` (sk_ only) finds or creates the Merchant's one CLI Webhook endpoint for the key's mode (`kind: "cli"`, `url: "cli://"`, `events: ["*"]`, a `whsec_` like any endpoint) and returns `{id: clis_…, endpoint_id, signing_secret, stream_url, livemode, merchant_name}`. The secret is returned on every call (the CLI prints it at start, CLI BR-CLI-002); the reveal is audited (FR-API-006). The endpoint object gains `kind: "http" \| "cli"`; `url`/`events`/`disabled` updates, roll and delete are refused for `kind: cli` (`400 invalid_request_error`). `test` is allowed on the CLI endpoint while `cli_connected_until > now()` and refused with "Start `elapse listen` first" otherwise ([ADR 2026-09-06 test delivery on CLI](../decisions/2026-09-06-test-delivery-on-cli-endpoint.md)). | Two calls return the same `endpoint_id` and secret; live key → live endpoint; `GET /v1/webhook_endpoints` lists it with `kind: cli`. |
| FR-API-131 | `GET /v1/cli/sessions/:id/stream` (sk_ bearer, `text/event-stream`) sends `event: delivery` frames `{id: dlv_…, event_id, type, created, headers, raw_body}` for each `queued` Delivery of the CLI endpoint (and each with a pending manual resend), signed at send time with the endpoint's current secret through the worker's signing helper, `headers` = `X-Elapse-Signature`, `X-Elapse-Delivery`, `Content-Type`; `event: heartbeat` every 15 s; `Last-Event-ID` on reconnect re-sends frames still unacked. While a stream is open the handler bumps `webhook_endpoints.cli_connected_until = now() + 60 s` on every frame and heartbeat. | Frame bytes equal `events.raw_body`; the signature verifies with `constructEvent` and the returned secret; reconnect with `Last-Event-ID` replays the unacked frame once. |
| FR-API-132 | `POST /v1/cli/sessions/:id/deliveries/:dlv/ack {status_code?, error?, duration_ms, printed_only?}` writes one `delivery_attempts` row (`actor: "cli"`, `request_headers` = the frame's headers, `status_code`/`error`, `duration_ms`) and sets the Delivery `succeeded` for 2xx or `exhausted` otherwise (single attempt; no retry schedule for CLI Deliveries). A second ack for the same Delivery → `409`. | Attempt row asserted; dashboard `GET /v1/deliveries/:id` shows the local status code; double ack → `409`. |
| FR-API-133 | `POST /v1/events/:id/resend` (sk_; Event-level, Stripe's `events resend`) requests a manual attempt on every existing Delivery of the Event (`manual_requested_at`, FR-WRK-030), including the CLI endpoint's Delivery when `cli_connected_until` is in the future, and returns `{object: "list", data: [Delivery summary…]}`. Audited. Per-Delivery `POST /v1/deliveries/:id/resend` (FR-API-064) is unchanged and remains what the dashboard button calls. | N Deliveries → N `manual_requested_at` set; unknown or other-mode Event → `404`. |
| FR-API-134 | Delivery creation (FR-API-073) matches a `kind: cli` endpoint only when its `cli_connected_until > now()` (CLI FR-CLI-018); the worker never claims CLI Deliveries (worker FR-WRK-001 amendment); a CLI Delivery still `queued` 10 minutes after creation is set `skipped` with `error: "cli_not_acked"`, by the stream handler on its poll and by the worker's tick. CLI endpoints are exempt from auto-disable (FR-WRK-050) and from live-mode URL validation (FR-API-062). | Event with no stream open → no CLI Delivery; stale row → `skipped` (clock injected); worker claim query returns no `kind: cli` rows. |
| FR-API-135 | Dashboard search ([ADR 2026-09-09 search as you type](../decisions/2026-09-09-dashboard-search-as-you-type.md)): `GET /v1/dashboard/search?q=` (dashboard session, current mode). `q` 2–254 characters after trim, else `400`. Returns `{object:"list", data:[{type, id, label, detail}]}`, at most five rows, ordered products, customers, subscriptions, events, endpoints, checkout sessions. A row matches when its id starts with `q` (case-sensitive) or `q` appears case-insensitively in a customer email or a product name; events and endpoints match on id only. `label` is the product name, customer email, or the id; `detail` is one line of context (rate, status, event type, endpoint host). Never a key, secret, or payload; never another merchant's or the other mode's rows. | One row per object type from a fixture; partial id and partial email both match; another merchant's objects never appear; `q` of one character → `400`; the same fixture in the other mode → empty. |

### Ingest from the indexer (doc §5.2 step 2–3, §15 "idempotent on txHash+logIndex")

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-070 | `POST /internal/ingest` authenticated with `Authorization: Bearer $INGEST_TOKEN` (shared secret, not a Merchant key). Body: `{chain_id, block_number, block_hash, block_timestamp, tx_hash, log_index, address, event_name, args}`. Unique index on `(chain_id, tx_hash, log_index)`; a duplicate returns `200 {duplicate: true}` and does nothing else. | Post the same body twice → one `chain_events` row, one Event. |
| FR-API-071 | Mapping (stream `address` → Subscription): `StreamStarted` → `active`, `started_at`; Events `checkout.session.completed` + `subscription.created`. **Amended 2026-09-14 (merchant-started metering):** when the stream was created by `createWithPermitNoStart`, `Deposited` completes the session and emits `checkout.session.completed` + `subscription.created` with the Subscription `incomplete`; the later `StreamStarted` sets `active`/`started_at` and emits **`subscription.updated`**. No new event type is introduced — the frozen six stand, and `subscription.updated` already carries lifecycle transitions. `StreamPaused r=0`/`StreamResumed` → `paused`/`active`; `subscription.updated`. `StreamPaused r=1` no longer emitted (contracts FR-CON-041); a cap end arrives as an ordinary `StreamCanceled` with `seconds_elapsed == max_duration_seconds` → `canceled`, `ended_reason: "cap_reached"`; Events `invoice.payment_failed` (+ FR-API-051) **and** `subscription.canceled`, in that order, plus the `payment_failed` notification (FR-API-109). `Settled` → Invoice + `invoice.settled`. `StreamCanceled` → `canceled`, `canceled_at`, `seconds_elapsed`, `amount_settled`; `subscription.canceled`. `StreamCreated`, `Deposited` → update `stream_address`/`funded_usd`, no merchant Event. Every body's `ledger` array (indexer FR-IDX-014) is written to `ledger_entries` (FR-API-107). | One fixture per chain event; assert Events created, Subscription fields, and ledger rows. |
| FR-API-072 | Ingest for an unknown `address` (not a platform-created stream) is stored and ignored (`200 {ignored: true}`). Ingest for a mode mismatch (chain 10143 event on a live Subscription) → `409`. | Tests. |
| FR-API-073 | Event creation and Delivery job creation happen in one Postgres transaction (worker FRD FR-WRK-001), so an Event never exists without its jobs. | Transaction rollback test. |
| FR-API-074 | `GET /v1/status` (public): `{chain_id, contracts{factory}, indexer{latest_block, head_block, lag_seconds, last_ingest_at}, worker{queued, oldest_queued_age_s}}` for judge mode (checkout FRD FR-CHK-011; indexer FRD FR-IDX-040). | Returns within 500 ms with mocked indexer. |
| FR-API-075 | **Relayer runway on `GET /v1/status`** (William 2026-09-08; [ADR 2026-09-08 keeper cadence](../decisions/2026-09-08-keeper-cadence-one-hour.md) decision 2): the response gains `relayer{address, balance_mon, sampled_at, burn_mon_per_hour, hours_left, low, stale}` read from `relayer_balance_samples` (worker FR-WRK-074), never from the RPC on request. `balance_mon` is a decimal string from the newest sample; `burn_mon_per_hour` is the balance drop between the newest sample and the oldest sample within the last hour, scaled to an hour, `null` with fewer than two samples or when the balance rose (a top-up); `hours_left = balance ÷ burn`, `null` when burn is `null` or zero; `low = hours_left < RELAYER_LOW_HOURS (24) OR balance_mon < RELAYER_LOW_MON (1)`; `stale = true` when the newest sample is older than 120 s (then `low` is still computed from it, so a dead worker with a low balance still alerts). With no sample at all the block is `{address, balance_mon: null, …, low: false, stale: true}`. The block is the UptimeRobot keyword target: a keyword monitor on `"low":true`. The address is the relayer's public keeper address, allowed here because the route is merchant-side (BR-CHK-001 applies to subscriber surfaces only). | Seeded samples: balance 4.64 with 0.024 drop per hour → `hours_left ≈ 193`, `low false`; 4.64 → 2.0 over an hour → `hours_left < 24`, `low true`; balance 0.5 flat → `low true` by floor; a rise → `burn null`, `low false`; newest sample 3 min old → `stale true`; no samples → nulls, `low false`. Response still within 500 ms. |

### Dashboard route group (dashboard FRD decisions 1, 2, 9, 10, 11, 14, 15; [ADR 2026-09-03 dashboard scope](../decisions/2026-09-03-dashboard-scope.md))

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-100 | Magic link: `POST /v1/dashboard/auth/magic_link {email}` always returns `200 {sent: true}` (no account enumeration), sends a single-use link valid **15 min** through the email provider (Undecided 10), rate-limited to 5 per email per hour and 20 per IP per hour. `POST /v1/dashboard/auth/verify {token}` consumes the token, sets the session cookie (FR-API-101), writes `audit_log(action: sign_in)`, and returns the Merchant. A first-time email creates the Merchant (`mrc_…`, Undecided 8) in the same call. `POST /v1/dashboard/auth/sign_out` clears the cookie. | Token reuse → `401`; expired → `401`; unknown email still `200`; sixth send in an hour → `429`. |
| FR-API-101 | Session cookie `elapse_session`: opaque id → `dashboard_sessions` row, `HttpOnly; Secure; SameSite=Lax; Path=/`, 7-day idle expiry, rotated on verify. Every `/v1/dashboard/*` route requires it; mutating routes additionally require an `Origin` header equal to the dashboard origin (CSRF). Mode is the `X-Elapse-Mode: test\|live` header, default `test` (dashboard FR-DSH-004). | Missing cookie → `401`; wrong `Origin` on `POST` → `403`; mode header scopes a list. |
| FR-API-102 | Merchant resources under cookie auth: the existing `/v1/products`, `/v1/customers`, `/v1/subscriptions`, `/v1/invoices`, `/v1/webhook_endpoints`, `/v1/events`, `/v1/deliveries` and `/v1/api_keys` routes accept the session cookie **or** a bearer `sk_` (one auth middleware, two credential kinds), so the dashboard reuses the merchant handlers and no data endpoint is duplicated. Only dashboard-only concerns live under `/v1/dashboard/*`. Cookie-authenticated calls are tagged `actor: dashboard` in the audit log and are excluded from `openapi.json`'s public operations. | Same product list via cookie and via `sk_`; cookie on `/internal/*` → `401`. |
| FR-API-103 | `GET /v1/dashboard/me` → Merchant profile (`name, email, support_email, support_url, payout_address, fee_bps, branding{display_name, logo_url, accent, support_url}, notifications{endpoint_exhausted_email, key_expiry_email}, checklist{key_created, product_created, checkout_opened, first_delivery_succeeded}`). `POST /v1/dashboard/me` updates profile, branding and notification switches. `fee_bps` is read from the factory (contracts FR-CON-006) and cached 60 s; the dashboard never hard-codes it (FR-DSH-061, FR-DSH-102). | Snapshot; `fee_bps` follows a mocked factory value. | **Amended 2026-09-07 ([ADR 2026-09-07 forms hardening](../decisions/2026-09-07-forms-hardening-logo-png-revoke-confirm.md)):** `support_url` and `branding.support_url` accept `http://` and `https://` only, `max(2048)`; `support_email` and `support_url` accept `null` to clear.
| FR-API-104 | Logo upload ([ADR 2026-09-07 forms hardening](../decisions/2026-09-07-forms-hardening-logo-png-revoke-confirm.md), amends the 2026-09-05 text): `POST /v1/dashboard/branding/logo` multipart, **PNG only, ≤ 50 KB** (FR-DSH-103). The server checks the PNG signature bytes (`89 50 4E 47 0D 0A 1A 0A`), never the file name or declared type; SVG and every other type → `400 invalid_request_error param=logo` ("Use a PNG under 50 KB."). Stored as Postgres `bytea` (Undecided 9) and served from `GET /v1/dashboard/branding/logo/:merchant_id` (public, `Cache-Control: public, max-age=31536000, immutable`, `Content-Type: image/png`, `X-Content-Type-Options: nosniff`); `branding.logo_url` becomes that URL with a content hash query so a replaced logo changes URL. `DELETE /v1/dashboard/branding/logo` clears it. Checkout sessions carry the URL (FR-API-030). | Oversize → 400; SVG/JPEG/renamed file → 400; happy path returns `logo_url` that fetches as `image/png`; delete clears `logo_url`. |
| FR-API-105 | Roll with grace (dashboard decision 9): `POST /v1/api_keys/:id/roll {grace: 0\|3600\|86400}` and `POST /v1/webhook_endpoints/:id/roll_secret {grace: 0\|3600\|86400}`. The old key's `expires_at` / the endpoint's `previous_secret_expires_at` = now + grace; grace 0 revokes or nulls immediately. Any other value → `400`. Supersedes the fixed window in FR-API-003 and FR-API-061 (Undecided 5 closed). | Three grace values tested; `expires_at` deltas exact; worker FR-WRK-040 reads the same column. |
| FR-API-106 | Payout address change: `POST /v1/dashboard/payout_address {address, confirm}` requires `confirm == address` (re-typed, FR-DSH-101), validates a checksummed EVM address, writes `audit_log(action: payout_address_changed)`, and applies to streams created afterwards only (Open item on `merchant` in `StreamFactory.create`). | Mismatch → `400`; audit row; existing subscription `stream_address` unchanged. |
| FR-API-107 | Ledger: `GET /v1/dashboard/ledger?kind=&subscription=&from=&to=` (paginated per FR-API-080) and `?format=csv` (same columns). Rows come from `ledger_entries`, written at ingest from the indexer's `ledger` array (indexer FR-IDX-014): `kind deposit\|settlement\|fee\|refund, amount (decimal string), subscription_id, customer_id, tx_hash, log_index, block_timestamp, reversed_by`. Never updated except to set `reversed_by` (FR-IDX-033). Totals per kind for the filtered range are returned in `summary`. | Four kinds from the kill-gate fixture; CSV byte-matches the JSON rows; a reversed row is excluded from `summary`. |
| FR-API-108 | Balance: `GET /v1/dashboard/balance` → `{payout_address, balance_usd (AUSD `balanceOf(payout_address)` via viem, cached 10 s), settled_this_month_net_usd, explorer_url}`; `404 no_payout_address` when unset (FR-DSH-120). No withdraw endpoint for 13 Oct (decision 12, FR-DSH-121). | Mocked chain read; empty-state error shape. |
| FR-API-109 | Notifications: `notifications(id, merchant_id, livemode, kind endpoint_exhausted\|key_expiring\|secret_expiring\|payment_failed\|first_delivery_succeeded, summary, target_id, emailed_at, read_at, created_at)`. Written by the worker (FR-WRK-042, FR-WRK-050), by ingest on `StreamPaused reason=1`, and by the first `succeeded` delivery. `GET /v1/dashboard/notifications` (unread count for the current mode plus `other_mode_unread`), `POST /v1/dashboard/notifications/read_all`. Emails go only for `endpoint_exhausted` and `key_expiring`/`secret_expiring` when the Merchant's switch is on; `emailed_at` records the send (FR-DSH-132). | Row per kind from fixtures; `read_all` clears the count for one mode only; email mock not called when the switch is off. |
| FR-API-110 | Activity: `GET /v1/dashboard/activity?action=&from=&to=` and `?format=csv` over `audit_log` (FR-API-006), newest first, actions extended with `sign_in`, `payout_address_changed`, `test_data_deleted`, `endpoint_reenabled`. Rows never contain secret values, only ids and `last4` (FR-DSH-142). | Table test; CSV; grep for `sk_`/`whsec_` beyond the prefix is empty. |
| FR-API-111 | Delete test data: `POST /v1/dashboard/test_data/delete {confirm_name}` requires `confirm_name == merchant.name`, deletes every `livemode = false` row for the Merchant in one transaction (products, customers, checkout sessions, subscriptions, invoices, endpoints, events, deliveries, ledger, notifications; test keys stay), writes an audit row. Live rows are untouchable by this route. | Counts drop to zero in test, unchanged in live; wrong name → `400`. |
| FR-API-112 | Stats for Home (FR-DSH-021): `GET /v1/dashboard/stats` → `{meters_running_now, accrued_today_usd, settled_this_week_net_usd, failed_payments_this_week}` for the current mode, computed in SQL, cached 10 s. | Values match fixtures. |
| FR-API-113 | CSV formula guard ([ADR 2026-09-07 forms hardening](../decisions/2026-09-07-forms-hardening-logo-png-revoke-confirm.md)): every `?format=csv` export (FR-API-107/110) prefixes a cell whose text begins with `=`, `+`, `-`, `@`, tab or CR with a single quote before the existing quoting, so a merchant-typed value never executes in a spreadsheet. | Unit test on the CSV encoder with each prefix; a negative number column is exempt only where the column is numeric. |

### Subscriber account routes (checkout FRD Surface 4, FR-CHK-016–026; dashboard decision 16)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-120 | Subscriber auth is a **Privy identity token** ([ADR 2026-09-07 identity token](../decisions/2026-09-07-privy-identity-token-on-prepare.md), William 2026-09-07): the page sends `X-Privy-Token: <identity token>` alongside the checkout-origin pass (FR-API-032). The API verifies it offline with Hono's `jwt` helper (ES256) against `PRIVY_VERIFICATION_KEY`, then checks `iss = privy.io`, `aud = PRIVY_APP_ID`, and `exp` (60 s skew). The subscriber's wallet is the token's linked account with `type: wallet`, `chain_type: ethereum`, `wallet_client_type: privy` (the embedded wallet); external wallets are ignored and a token with no embedded wallet is refused. The Customer's email is the token's `email` account, else its `google_oauth` account's email, else none; the page never sends an email. No Elapse-issued subscriber credential exists (checkout FRD FR-CHK-016). Every token failure (missing, malformed, bad signature, wrong issuer or audience, expired, no embedded wallet) is one `401 authentication_error code=subscriber_auth_invalid` with a single message, so the response never says which check failed. The token is never logged. | Unit tests with a locally generated P-256 key pair: valid token → address and email; each failure mode → the one 401; external-wallet-only token → 401; verifier uses no network. |
| FR-API-121 | `GET /v1/account/subscriptions?status=` returns every Subscription whose Customer's `wallet_address` equals the caller's, across Merchants, each with `merchant{name, logo_url, support_url}`, `product`, `status`, `started_at`, `rate_usd_per_second`, `funded_usd`, `settled_usd`, `seconds_elapsed`. Index `customers(wallet_address)` added. Merchant fields are the public branding only; never keys, endpoints, or payout addresses. **Amended 2026-09-07 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)):** auth is the Privy identity token (FR-API-120) from the checkout origin; `status` accepts `active`, `paused`, `canceled` or a comma list, default all three; each row also carries `livemode`, `seconds_elapsed`, `settled_usd`, `refunded_usd`, `ended_reason`, `max_duration_seconds`, `funded_usd`, `pending_tx`, `paused_at`, `canceled_at`; `incomplete` never appears; newest `started_at` first, at most 200 rows. **Amended 2026-09-07 ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md), William):** each row also carries `checkout_session` (the `cs_` id) so the account receipt can call FR-API-126. | Two merchants, one wallet → both rows; another wallet → none. |
| FR-API-122 | `GET /v1/account/invoices?subscription=` returns the caller's settlements across Merchants with `merchant.name`, `seconds`, `amount_settled`, `period_start/end`, for the receipts list (FR-CHK-020). Fee is never shown to subscribers; `amount_settled` is gross, which is what they paid. **Deferred 2026-09-07 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)):** not needed for `/account` (receipts come from canceled subscriptions in FR-API-121); not built for October. | Rows scoped to the wallet; no `fee` field in the projection. |
| FR-API-123 | `POST /v1/account/subscriptions/:id/cancel {signature}` submits `cancel()` for the subscriber party through the relayer with the subscriber's signed authorisation (contracts FR-CON-017), `202` with `pending_tx`; `canceled` arrives via ingest (BR-API-005). `POST /v1/account/receipts/:invoice_id/email` sends the receipt email (Undecided 10). **Amended 2026-09-07 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)):** `POST /v1/account/subscriptions/:id/cancel/prepare` and `…/cancel {signature, deadline}` mirror the session routes (FR-API-032 cancel) with the identity token instead of the session pass, reusing the same service through the subscription's checkout session; another wallet's subscription → `404`. `POST /v1/account/subscriptions/:id/receipt/email` sends the receipt (merchant, product, seconds, paid, returned, started, stopped, no fee, no chain words) through the mailer to the identity's email for a `canceled` subscription of the caller's wallet; `200 {sent: true}`; more than one per subscription per 10 min → `429 rate_limit_error`; a running subscription → `400`. | `202`; subscription of another wallet → `404`; bad signature → `400`. |
| FR-API-125 | Unconfigured Privy ([ADR 2026-09-07 identity token](../decisions/2026-09-07-privy-identity-token-on-prepare.md)): when `PRIVY_APP_ID` or `PRIVY_VERIFICATION_KEY` is unset the API still starts and every merchant route works, but `prepare` and `cancel/prepare` answer `503 api_error code=subscriber_auth_unconfigured` naming the two variables. Nothing ever falls back to trusting a wallet address from the page. | Both variables unset → 503 on the two routes, 200 elsewhere; CI's Quickstart job (which never calls prepare) is unaffected. |
| FR-API-126 | Start again ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md)): `POST /v1/checkout/sessions/:id/again` from the checkout origin with the Privy identity token (FR-API-120). The token's wallet must be the session's Customer (`403 subscriber_mismatch`), the session's Subscription must be `canceled` (`409 not_ended`), and the Product must be active (`400 product_archived`, "This product is no longer available."). Creates a new open session for the same merchant, mode, product, `success_url`, `cancel_url` and merchant-fixed `max_duration_seconds`, bound to the same Customer, with `last_max_duration_seconds` carrying the ended session's cap for the page to preselect; returns `201 {id, url}`. Audit row `checkout_session.again` with both ids. The merchant is told by `checkout.session.completed` when the new meter starts, as for any session. Rate: 10 per session per hour. **Amended 2026-09-07 (William):** a session that already has a follow-on answers `409 already_restarted` with the follow-on's id; the public session projection (FR-API-031) and each account row (FR-API-121) carry `restarted_as` (`cs_` id or null), the newest follow-on. | Route tests: canceled → 201 with a new id and the copied fields; running → 409; archived → 400; stranger → 403; the public projection of the new session carries `last_max_duration_seconds`. |
| FR-API-138 | **`/account` lists held money** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). `GET /v1/account/subscriptions` (FR-API-121) also returns `incomplete` subscriptions that are `merchant`-mode and funded (`stream_address` set and `funded_wei > 0`); unfunded and `checkout`-mode `incomplete` rows stay hidden. Every account row carries `start_mode` and `start_by` (FR-API-137). `POST /v1/account/subscriptions/:id/cancel` (FR-API-123) accepts such a row and stamps `cancel_submitted_at` in the same way. | Listing includes a funded `merchant` `incomplete` row and excludes an unfunded one and a `checkout`-mode one; cancel on the held row → `202` and the stamp. |
| FR-API-124 | Withdrawn 2026-09-04 ([ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md)): no balance, top-up, add-funds, or return-to-card endpoints exist. The subscriber's AUSD stays in their wallet; the API never holds or moves it beyond a signed permit. | `grep` for `/account/balance` in routes is empty. |

### Conventions (Stripe-like; doc §3 "Mirror Stripe")

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-080 | Lists: `?limit=` (1–100, default 10), `?starting_after=<id>`, response `{object:"list", data:[], has_more, url}`; newest first. | Cursor test across 25 rows. |
| FR-API-081 | `Idempotency-Key` header on every `POST`: same key + same body within 24 h → replay the stored response; same key + different body → `400 idempotency_error`. | Tests. |
| FR-API-082 | Error shape `{error: {type: api_error|authentication_error|invalid_request_error|rate_limit_error|idempotency_error|not_found, code?, message, param?}}`; ids that exist in another mode return `404`, never `403`. One exception: `cancel/prepare` with a verified token whose embedded wallet is not the session's Customer → `403 authentication_error code=subscriber_mismatch` ("Signed in as a different subscriber."), because the page must show sign-in, not retry (FR-CHK-027). | Snapshot tests. |
| FR-API-083 | Wire format is `snake_case` JSON (§5.3); the TS SDK maps `rateUsdPerSecond → rate_usd_per_second`, `successUrl → success_url` (Undecided 2). Ids carry object prefixes; every object has `object` and `created`. | Contract test between `sdk/ts` and `openapi.json`. |
| FR-API-084 | `openapi.json` is generated from route schemas (Zod/`@hono/zod-openapi` or equivalent), served at `GET /openapi.json`, and published into docs (§12 Week 4). Every SDK method in §4.2 has exactly one operation; no operation exists that the SDK/docs do not list, except dashboard-internal and `/internal/*`. | CI diff between SDK method list and OpenAPI operationIds. |
| FR-API-085 | **Committed public OpenAPI file** ([ADR 2026-09-06 docs site](../decisions/2026-09-06-docs-site-mintlify-and-quickstart-ci.md), docs FR-DOC-041): the nine public route definitions (`products.create/retrieve/list`, `checkout.sessions.create`, `subscriptions.retrieve/list/cancel`, `customers.retrieve`, `invoices.list`) carry a `public` marker; `bun run openapi` writes `api/openapi.json` containing only marked operations, tagged by resource (Products, Checkout, Subscriptions, Customers, Invoices), with a `bearerAuth` security scheme (`sk_test_…` / `sk_live_…`), the error envelope of FR-API-082 as shared responses, and `servers[0].url` = the hosted API URL from `PUBLIC_API_URL` (the local default when unset). A test regenerates in memory and fails when the committed file differs; a second test asserts the marked set equals the SDK's exported methods (the CI diff FR-API-084 asks for). The live `GET /openapi.json` is unchanged and stays complete. | `bun test` fails after a public route change until `bun run openapi` is rerun; nine `operationId`s exactly; no `dashboard.*`, `cli.*`, `apiKeys.*`, `webhookEndpoints.*`, or `checkout.sessions.prepare/start/cancel` in the file. |
| FR-API-086 | **Docs origin** (docs FR-DOC-045): CORS for `DOCS_ORIGIN` (env, unset = off) on `/v1/*` public routes with `Authorization` allowed, so the reference's try-it panel works with a test key. A request whose `Origin` is the docs origin and whose bearer is `sk_live_…` → `401 authentication_error`, code `live_key_in_browser`, message "Live keys cannot be used from a browser." No other origin gains CORS. | Preflight from `DOCS_ORIGIN` → 204 with the header; `sk_live_` + that `Origin` → 401; `sk_live_` without `Origin` unchanged. |

### Test clocks (doc §6 item 9 "fast-forward demo rates for the hackathon video")

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-API-090 | Test mode only: `POST /v1/test_helpers/test_clocks {frozen_time}` → `clock_…`; `POST …/advance {frozen_time}`; a Checkout session/Subscription created with `test_clock` derives `seconds_elapsed`, `settled_usd` and lifecycle from the clock rather than wall time, and emits the same Events via the same Event/Delivery path (no chain). Live keys → `400`. | Advance 3 600 s → `seconds_elapsed == 3600`, one `invoice.settled` Event. |
| FR-API-091 | Test-clock Subscriptions are marked `simulated: true` and never call the relayer or the chain (Undecided 4). | No viem call in test. |

**Not built for 13 October** (William 2026-09-06, CLI grill Q4): the demo cancels after real seconds; FR-API-090/091 stay in the spec for a post-hackathon revisit and the docs page becomes "Testing".

## Business rules

| Id | Rule |
| --- | --- |
| BR-API-001 | Test and live data never mix: every table has `livemode`; every query is scoped by the key's mode (BR-DSH-002). |
| BR-API-002 | The indexer never holds Merchant secrets; ingest uses one platform token and carries no Merchant identifiers (§5.2). |
| BR-API-003 | Secret keys and webhook secrets are shown once; the API never returns them again (BR-DSH-001). |
| BR-API-004 | Money is decimal strings on the wire and `NUMERIC`/bigint in Postgres; never floats (BR-MTR-001). `rate_usd_per_second` must be exactly representable in token base units or is rejected (BR-CON-005). |
| BR-API-005 | Subscription status is derived only from ingested chain events (or the test clock); an API call can request a change but never sets `active/canceled` itself. |
| BR-API-006 | Only the six §5.1 event types exist. No `invoice.tick`; nothing per second (§5.1). `invoice.payment_failed` now means "the session ended because its cap was reached" (FR-API-051), the only shape a payment failure takes once escrow is pre-funded by permit. |
| BR-API-007 | The frozen §4.2 surface is servable end-to-end by Week 2 (`products.create`, `checkout.sessions.create`, `constructEvent`) and in full by Week 4. |
| BR-API-008 | `amount_settled` and `seconds_elapsed` in Events are cumulative totals for the Subscription, matching `StreamCanceled` (FR-CON-024). |

## Data / interfaces (Postgres)

```
merchants(id mrc_…, name, email, support_email, support_url, payout_address, branding jsonb, notify_endpoint_exhausted bool, notify_key_expiry bool, created_at)
dashboard_sessions(id, merchant_id, created_at, last_seen_at, expires_at)   magic_links(token_hash, email, expires_at, used_at)
ledger_entries(id led_…, merchant_id, livemode, kind, amount_wei, subscription_id, customer_id, tx_hash, log_index, block_hash, block_timestamp, reversed_by)   UNIQUE(chain_id, tx_hash, log_index, kind, block_hash)
notifications(id, merchant_id, livemode, kind, summary, target_id, emailed_at, read_at, created_at)
api_keys(id, merchant_id, livemode, kind pk|sk, name, hash, last4, created_at, last_used_at, revoked_at, expires_at)
products(id, merchant_id, livemode, name, description, rate_usd_per_second NUMERIC(38,18), rate_per_second_wei NUMERIC(78,0), allow_pause, active, created_at)
customers(id, merchant_id, livemode, email, passkey_id, wallet_address, created_at)          UNIQUE(merchant_id, livemode, wallet_address)  INDEX(wallet_address)
checkout_sessions(id, merchant_id, livemode, product_id, customer_id, subscription_id, success_url, cancel_url, status, expires_at, test_clock_id, created_at)
subscriptions(id, merchant_id, livemode, product_id, customer_id, status, stream_address, chain_id, started_at, paused_at, canceled_at,
              max_duration_seconds, max_escrow_wei, funded_wei, settled_wei, settled_seconds, permit_nonce, permit_deadline, ended_reason canceled|cap_reached,
              simulated, test_clock_id, created_at)                     UNIQUE(chain_id, stream_address)
invoices(id, merchant_id, livemode, subscription_id, period_start, period_end, seconds, amount_wei (gross), fee_wei, status paid|failed, tx_hash, log_index, created_at)
webhook_endpoints(id, merchant_id, livemode, url, events text[], disabled, disabled_reason, secret_enc, previous_secret_enc, previous_secret_expires_at, consecutive_failures, failing_since, warned_24h_at, created_at)
events(id, merchant_id, livemode, type, data jsonb, raw_body text, created, pending_webhooks, chain_event_id)
deliveries(id, event_id, endpoint_id, status queued|retrying|succeeded|exhausted|skipped, attempt, next_attempt_at, locked_until, created_at)   -- `manual` is an attempt flag, not a status (worker FRD, 2026-09-05)
delivery_attempts(id, delivery_id, n, manual bool, actor, sent_at, duration_ms, status_code, error, request_headers jsonb, response_excerpt)
chain_events(id, chain_id, block_number, block_hash, block_timestamp, tx_hash, log_index, address, event_name, args jsonb, received_at)   UNIQUE(chain_id, tx_hash, log_index)
idempotency_keys(key, merchant_id, request_hash, response jsonb, created_at)   audit_log(id, merchant_id, actor, action, target, ip, at)
```

Env: `DATABASE_URL`, `MONAD_RPC_URL`, `CHAIN_ID`, `RELAYER_PRIVATE_KEY` (holds MON for gas only, never AUSD; the only key that can open a stream — [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md)), `INGEST_TOKEN`, `WEBHOOK_SECRET_KEK` (encryption key for `secret_enc`), `NEXT_PUBLIC_CHECKOUT_URL`.

## Undecided (human)

1. **Framework.** Decided 2026-09-03 (Furqaan): **Bun + Hono**, separate `api/` service; the worker shares its codebase and runs as a second Bun process.
2. ~~**Wire casing.**~~ **Decided 2026-09-05 (William): (a)** `snake_case` on the wire. The SDK accepts the frozen camelCase request keys and passes responses through untouched, so the cURL tab and the webhook body match byte for byte (FR-API-083).
3. ~~**Pause/resume exposure.**~~ **Confirmed 2026-09-05: (a)** dashboard-internal only, hidden from OpenAPI and the SDK (dashboard decision 7, SDK FR-SDK-007). The contract accepts pause from either party (contracts FR-CON-050); no surface offers merchant pause. **Revised 2026-09-07** ([ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md)): the dashboard-internal routes never existed and are withdrawn (FR-API-043 superseded); pause and resume are subscriber actions under the session and the account (FR-API-044..047), relayed with the party's signature, still hidden from OpenAPI and the SDK.
4. ~~**Test mode semantics.**~~ **Decided 2026-09-05 (William): (a)** test keys create real streams on Monad testnet 10143 with `MockUSD`; live keys use 143 and AUSD. **Amended 2026-09-07 (William):** until a chain-143 record exists, live keys also use 10143 and `MockUSD` (`config.chains.live = 10143`), see [ADR 2026-09-07 testnet submission](../decisions/2026-09-07-submission-on-testnet-live-mode-mockusd.md). Test clocks exist only for explicitly `simulated` Subscriptions (FR-API-090). Gas is never a user's problem in either mode: the relayer pays (permit ADR). In test mode the relayer also **mints `MockUSD` to the subscriber inside the checkout transaction** (FR-API-032), so no one hunts for a faucet; the relayer's own testnet MON is refilled from the faucet (≈ 5 MON / 12 h ≈ 100 checkouts a day), a platform chore recorded in `api/README.md`. **Amended 2026-09-13** ([ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md)): the mint is gone with MockUSD; both modes escrow testnet AUSD, and demo wallets are funded by the team from the holding wallet.
5. ~~**Key roll grace window.**~~ **Decided 2026-09-03 (dashboard decision 9):** the Merchant chooses per roll, 0 / 1 h / 24 h (FR-API-105).
6. ~~**Rate limits.**~~ **Decided 2026-09-05 (William):** 100 req/s per `sk_`, 20 req/s per `pk_`, 10 `POST /v1/checkout/sessions` per second per Merchant (FR-API-005). Config values, not code.
7. ~~**Webhook secret at rest.**~~ **Decided 2026-09-05 (William): (a)** encrypted with `WEBHOOK_SECRET_KEK` from the server environment (FR-API-060); the worker decrypts at send time (worker FR-WRK-023). API keys stay hashed (FR-API-002).
8. ~~**Merchant id prefix / dashboard auth.**~~ Magic link decided 2026-09-03 (dashboard decision 1, FR-API-100). **Prefix confirmed 2026-09-05: `mrc_`**, matching the signed dashboard spec and the built mock.
9. ~~**Logo storage.**~~ **Decided 2026-09-05 (William): (a)** Postgres `bytea`, served from `GET /v1/dashboard/branding/logo/:id` with long cache headers (FR-API-104). No bucket, no credential; revisit only if it matters.
10. ~~**Email provider.**~~ **Decided 2026-09-05 (William): Resend**, one provider for magic links, notifications and subscriber receipts. `RESEND_API_KEY` in the server environment; sending is a single HTTP call behind one `sendEmail()` so the provider can change without touching callers.
11. ~~**Hosting.**~~ **Decided 2026-09-05 (William):** the Bun API and the worker run on **Railway** as two processes from this repo; Postgres is **Neon** (serverless, free tier, branching for test data). Both deploy from GitHub. Recorded in `api/README.md`; the frontend stays on Vercel.
12. **Relayer key custody.** For testnet, `RELAYER_PRIVATE_KEY` lives in Railway's environment for the API process only, holds MON for gas and never AUSD, and is rotated by redeploying. Before mainnet (Week 5) it moves to a KMS or an HSM-backed signer, and the factory owner and treasury become a Safe multisig, distinct from the relayer. Recorded here so it is not forgotten; the mainnet half is for William and Furqaan.

## Open

- ~~`checkout.session.completed` timing~~ **Decided 2026-09-05 (William):** fires on `StreamStarted`, i.e. when the stream is running on chain. Under the permit design signing, escrow and start are one transaction, so there is no earlier meaningful moment; provisioning access on a signature the chain might reject is what Stripe also refuses to do.
- Ordering of the cap-end pair: `invoice.payment_failed` before `subscription.canceled` (FR-API-071) is a convenience only — deliveries carry no ordering guarantee (worker BR-WRK-004), so the docs must tell merchants to key on `evt_` id and read `data.object.status` rather than rely on arrival order.
- Merchant `payout_address` is the `merchant` argument to `StreamFactory.create`; changing it after streams exist only affects new streams — document.
- ~~Whether Customers and Invoices endpoints are dashboard-visible~~ **Yes** — both pages are built in the dashboard (FR-DSH-050s, FR-DSH-060s) and read these endpoints through the shared auth middleware (FR-API-102).

## Revision

| Date | Who | Change |
| --- | --- | --- |
| 2026-09-03 | Claude (for William) | First draft from the detailed doc and design brief. |
| 2026-09-04 | Claude (for William) | Dashboard dependencies applied: FR-API-100–112 dashboard route group (magic link, cookie session, shared auth middleware, branding, roll with grace, payout address, ledger, balance, notifications, activity, delete test data, stats); FR-API-120–124 subscriber account routes (FR-API-124 proposed pending the funding ADR); Invoice gross/fee/net; tables; Undecided 5 and 8 closed, 9–10 added. |
| 2026-09-04 | Claude (for William) | [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md) applied: FR-API-030 `max_duration_seconds`/`max_escrow_usd`; FR-API-032 prepare returns the permit payload, start submits it through the relayer; FR-API-123 signed cancel; FR-API-124 withdrawn; subscription columns; relayer env note. |
| 2026-09-04 | Claude (for William) | Cap end applied: FR-API-051 recognises a cap end from `seconds_elapsed == max_duration_seconds` and records `ended_reason`, FR-API-071 drops the `reason=1` mapping; the `invoice.payment_failed` question recorded as Open for the human. |
| 2026-09-04 | Claude (for William) | Cap end fires `invoice.payment_failed` with `subscription.canceled` (William's option 2): FR-API-051, FR-API-071, BR-API-006; the frozen §5.1 catalog keeps all six types. |
| 2026-09-04 | Claude (for William) | FR-API-041 becomes a public, SDK-backed operation for the new `subscriptions.list` method. |
| 2026-09-05 | Claude (for William) | Grill round: wire casing, test mode (real streams on testnet, relayer mints MockUSD at test checkout), rate limits, webhook secret at rest, logo storage, Resend, Railway + Neon hosting, `checkout.session.completed` timing all decided; pause/resume, `mrc_` and dashboard visibility confirmed; relayer key custody recorded (Undecided 12). Awaiting signature. |
| 2026-09-05 | William | Signed. Week 2 build begins against this spec. |
| 2026-09-05 | Claude (for William) | Delivery status model corrected to the worker FRD's (`skipped` status; `manual` and `actor` on attempts); endpoint `disabled_reason`, `failing_since`, `warned_24h_at` for time-based auto-disable ([ADR 2026-09-05](../decisions/2026-09-05-worker-in-api-and-auto-disable.md)). |
| 2026-09-05 | William | Schedule: the dashboard auth routes (FR-API-100, 101) and the key routes (FR-API-003, 105) are pulled forward from Week 4 to the end of Week 2 so merchants can mint keys over HTTP for the SDK quickstart; a `bun run seed-merchant` script prints a local test key for development. |
| 2026-09-05 | Claude (for William) | Ingest built (FR-API-070..073, FR-API-050/051, FR-API-107 write side): migration 0009 adds `customers`, `subscriptions`, `chain_events`, `invoices`, `ledger_entries`. Two details beyond the text: (1) `subscriptions.pending_tx` lets ingest bind `StreamCreated`/`Deposited`/`StreamStarted` to the Subscription by the relayer's tx hash before `start` has read the receipt, closing the race with a fast indexer; `start` (FR-API-032) must write `pending_tx` right after broadcast. (2) `subscriptions.paused_seconds` accumulates on `StreamResumed` so `seconds_elapsed` excludes pauses. Env `INGEST_TOKEN` (same value as the indexer's `ENVIO_INGEST_TOKEN`). The FR-API-072 `409` can only arise from an inconsistent row since lookups are per `chain_id`; kept as a guard. `GET /v1/status` (FR-API-074) not yet built. |
| 2026-09-05 | Claude (for William) | FR-API-032 built: `prepare {max_duration_seconds, wallet_address, email?}` and `start {signature}` under `pk_`; `PermitTypedData` per ERC-2612 with a 600 s deadline; relayer via viem behind a `ChainClient` seam (`RELAYER_PRIVATE_KEY`, `MONAD_RPC_URL`, `CHAIN_ID`); test mode mints MockUSD when the wallet is short; `pending_tx` written at broadcast. **Identity (William, option c):** the wallet address is trusted from the page and proven by the permit signature at `start`; a Privy identity token becomes required in Week 4 once the app id exists. `start` requires `merchants.payout_address` (`no_payout_address` → 400). Public session projection (`pk_`) now carries `customer {id, email}` and the `subscription` object; the `sk_` object carries ids. Deployment record copied into `api/deployments/` by `pnpm sync-deployments`. |
| 2026-09-05 | Claude (for William) | **Live proof on Monad testnet**: FR-API-030 → 032 → 070/071 → 073 → worker in one run. Subscriber prepare/sign/start, relayer `createWithPermit` (tx `0x5decd1d9…75e54b`), Envio → ingest bound `StreamCreated/Deposited/StreamStarted` by `pending_tx`, session `complete`, subscription `active`, two Events delivered `succeeded` and verified with `constructEvent`. `RELAYER_PRIVATE_KEY` accepted with or without `0x` (cast prints it bare). `api/.env.example` added. |
| 2026-09-05 | Claude (for William) | Subscriber cancel built under the session (`POST /v1/checkout/sessions/:id/cancel/prepare` → 32-byte message, `…/cancel {signature, deadline}` → relayer `cancelFor`, 202 + `pending_tx`; the account-page route FR-API-123 reuses the same service). Proven live: cancel at 24 s, `invoice.settled` + `subscription.canceled` delivered and verified. **Ordering fix (FR-API-071):** same-block logs arrive in any order (indexer BR-IDX-008); a `Settled` that lands after `StreamCanceled` no longer adds to the cumulative seconds/amount the cancel already set (fee still accumulates). **Open (William):** FR-API-042 merchant cancel says the relayer submits `cancel()` as the merchant party, but the on-chain merchant is the payout address the platform does not hold; see options in the 2026-09-05 conversation. |
| 2026-09-05 | Claude (for William) | FR-API-042 resolved by [ADR 2026-09-05 keeper may cancel](../decisions/2026-09-05-keeper-may-cancel.md): the relayer, as factory keeper, calls `cancel()` directly; no merchant signature. |
| 2026-09-05 | Claude (for William) | FR-API-041 `subscriptions.retrieve` and FR-API-042 `subscriptions.cancel` built (202 + `pending_tx`; `not_running` → 409). **Proven live on the redeployed factory**: sk_ cancel → keeper `cancel()` tx `0x7826a0c5…f7d7a` → `invoice.settled` + `subscription.canceled` verified 23.7 s after the start click, 19 s billed. `subscriptions.list` follows with the read slice. |
| 2026-09-05 | Claude (for William) | Cap end proven live end to end (FR-API-051): keeper `settleBatch` past the cap → `Settled` + `StreamCanceled` → `invoice.settled`, `invoice.payment_failed` (zero, `failed`), `subscription.canceled` with `ended_reason: cap_reached`, `payment_failed` notification; 60 s billed, $0.24, nothing refunded, no refund ledger row. Same-block logs arrived cancel-first and the totals stayed correct. |
| 2026-09-05 | Claude (for William) | Read slice built: FR-API-041 `subscriptions.list` (`status`/`customer`/`product` filters), FR-API-021 `customers.list/retrieve`, FR-API-052 `invoices.list/retrieve` (`subscription`/`customer`), all on the FR-API-080 keyset envelope (shared `lib/keyset.ts`). FR-API-074 `GET /v1/status` built: public, adds `block_time_ms: 400`, `contracts.token`, `indexer.ok/lag_blocks/unsent_events`; reads Envio's Hasura server-side (`INDEXER_GRAPHQL_URL`, default the `envio dev` endpoint; the public role has no aggregates, so unsent events are counted from a bounded list). Live: lag 0 blocks. The ten frozen SDK methods now all have a server. |
| 2026-09-05 | Claude (for William) | Hosted-page auth (option a): `requireAuth({checkout:true})` on the five session-scoped routes, session loaded by id, CORS for the checkout origin and `/v1/status`; `GET /v1/checkout/sessions/:id/deliveries` public delivery log for judge mode (type, last status, attempt, time; never URLs). An unfunded relayer answers `503 relayer_unfunded` (seen live 2026-09-06 after 4.5 MON of proofs: ~0.1 MON per checkout at ~100 gwei). |
| 2026-09-06 | Claude (for William) | FR-API-103 `GET/POST /v1/dashboard/me` built (migration 0012 `onboarded_at`; `name` null until first run; checklist per mode; `fee_bps` from the deployment record, 100 for live until Week 5). Dashboard CORS with credentials for `DASHBOARD_ORIGIN`. Reads gain dashboard fields, never in signed bodies: events `object_id`/`delivery_state` and `deliveries` on retrieve, `since`/`until`; deliveries `event_type`/`event_created`/`endpoint_url`/`max_attempts` and `?status=`; endpoints `success_rate_7d`. Magic link returns `dev_token` only when mail is stdout-only outside production. |
| 2026-09-06 | Claude (for William) | `GET /v1/dashboard/overview` (cookie) for Home; subscriptions gain `product_name`/`customer_email`; products gain `active_subscriptions`. |
| 2026-09-06 | Claude (for William) | Built: FR-API-106 payout address (re-typed confirm, audit row), FR-API-107 ledger with `summary` and `?format=csv`, FR-API-108 balance (token `balanceOf` at the payout address via the chain client, `404 no_payout_address`), FR-API-109 notifications list/unread/other-mode/read_all, FR-API-110 activity, FR-API-111 delete test data in one transaction. Customers gain `subscription_count`, `total_settled_usd` and `?search=`. |
| 2026-09-06 | Claude (for William) | CLI grill applied ([ADR 2026-09-06](../decisions/2026-09-06-cli-transport-and-session.md)): FR-API-130–134 CLI sessions (SSE stream, ack, Event-level resend, `kind: cli` endpoint with `cli_connected_until`, 10-minute expiry to `skipped`); endpoint object gains `kind`; test clocks FR-API-090/091 marked not built for 13 October. Awaiting William's confirmation as a post-signature addition. |
| 2026-09-06 | William | Confirmed FR-API-130–134 and the test-clock deferral as post-signature additions. |
| 2026-09-06 | Claude (for William) | FR-API-130–134 built (migration 0013: `webhook_endpoints.kind`, `cli_connected_until`, one CLI endpoint per mode, `cli_sessions`). Stream in `services/cli-stream.ts`: polls the CLI endpoint's queued rows every 500 ms, signs with `signPayload`, opening heartbeat after the first pass, then every 15 s; the ack echoes the frame's `headers` and `manual` flag so the attempt row carries the signature and a Resend is recorded as manual; a manual ack leaves status unchanged like the worker's. Expiry runs in the stream's poll and in a worker sweep every 60 s. Endpoint routes refuse update/delete/roll/test on `kind: cli` with `400`. |
| 2026-09-06 | Claude (for William) | Docs grill applied ([ADR 2026-09-06 docs site](../decisions/2026-09-06-docs-site-mintlify-and-quickstart-ci.md)): FR-API-085 committed public `api/openapi.json` with freshness and surface tests; FR-API-086 CORS for the docs origin with live keys refused from the browser. Awaiting William's confirmation as a post-signature addition. |
| 2026-09-06 | William | Confirmed FR-API-085 and FR-API-086 as post-signature additions. |
| 2026-09-06 | Claude (for William) | FR-API-085 built: `PUBLIC` marker (`x-public`) on the nine SDK routes, `src/openapi/public.ts` filters the live document, adds `bearerAuth`, the FR-API-082 `Error` schema on every operation and the five tags, prunes unreferenced schemas; `bun run openapi` writes `api/openapi.json` (`--check` for CI); tests derive the expected operation set from the SDK client itself. FR-API-086 built: `DOCS_ORIGIN` CORS on `/v1/*`, `sk_live_` from that origin → `401 live_key_in_browser` with CORS headers on the refusal. `PUBLIC_API_URL` feeds `servers`. The signed-body `Event` (new `lib/event-schema.ts`; the dashboard read schema extends it with `object_id`/`delivery_state`) is kept in the file although no public operation returns it, so the docs catalog validates its six sample payloads against it (FR-DOC-030) and can render "The Event object". |
| 2026-09-06 | Claude (for William) | Public operations gained `summary` (Mintlify page titles). `sampleObject` takes an optional clock so synced payloads are byte-stable. |
| 2026-09-06 | Claude (for William) | FR-API-130 amended per [ADR 2026-09-06 test delivery on CLI](../decisions/2026-09-06-test-delivery-on-cli-endpoint.md): `test` on the CLI endpoint while connected; endpoint row exposes `cli_connected_until`. Found in William's first Quickstart run. |
| 2026-09-07 | Claude (for William) | Grill applied ([ADR 2026-09-07 identity token](../decisions/2026-09-07-privy-identity-token-on-prepare.md)): FR-API-120 rewritten for the identity token (offline ES256 via Hono's `jwt`, embedded wallet and email from the token, one 401); FR-API-125 unconfigured → 503; FR-API-032 amended (`prepare` and `cancel/prepare` take the token, body `wallet_address`/`email` removed); FR-API-082 gains `403 subscriber_mismatch`. Awaiting signature. |
| 2026-09-07 | William | Signed FR-API-120/125 and the FR-API-032/082 amendments. |
| 2026-09-07 | Claude (for William) | Built FR-API-120/125 and the FR-API-032/082 amendments: `api/src/lib/privy.ts` (`verifyIdentityToken`, offline ES256 via `hono/jwt`, 60 s skew, embedded wallet and email from `linked_accounts`, one 401, 503 when unconfigured, `setPrivySettings` test seam); `X-Privy-Token` on `prepare` and `cancel/prepare`, body `wallet_address`/`email` removed, `403 subscriber_mismatch` on cancel/prepare; `PRIVY_APP_ID`/`PRIVY_VERIFICATION_KEY` in config and `.env.example`. Tests: `privy-identity.test.ts` (4), route tests migrated to a local Privy fixture (`test/privy-fixture.ts`); 275 pass. Public OpenAPI unchanged (the routes are hidden). |
| 2026-09-07 | Claude (for William) | FR-API-034 added (William chose option 1 of three: refuse a live start that the wallet cannot fund, before any gas is spent). Awaiting signature. |
| 2026-09-07 | William | Signed FR-API-034. |
| 2026-09-07 | Claude (for William) | Built FR-API-034: `startSession` reads `balanceOf` in both modes; live and short → `CheckoutStateError("insufficient_balance")` (400) with both figures in USD, no mint, no `createWithPermit`; test mode keeps the top-up. Test in `checkout-start.test.ts` points the live chain id at the testnet record for its duration, since no mainnet record exists yet. 276 pass. |
| 2026-09-07 | Claude (for William) | FR-API-035/036 added ([ADR 2026-09-07 payout gate](../decisions/2026-09-07-payout-address-gates-checkout-and-live-keys.md)): payout address optional at setup, required to create Checkout sessions and to issue live keys. Awaiting signature. |
| 2026-09-07 | William | Signed FR-API-035/036. |
| 2026-09-07 | Claude (for William) | Built FR-API-035/036: `checkout.sessions.create` → `400 no_payout_address` without an address (both modes); live `api_keys.create` → the same code, and the live list omits the publishable key until the address is set (a 200 list rather than a 400, so the Developers page can render the gate). Tests in `checkout-sessions.test.ts` and `dashboard-auth.test.ts`; 279 pass. The profile read already returns `payout_address`, so no `payout_address_set` field was added. |
| 2026-09-07 | Claude (for William) | Live mode on testnet ([ADR 2026-09-07 testnet submission](../decisions/2026-09-07-submission-on-testnet-live-mode-mockusd.md)): `config.chains.live` → 10143 until a mainnet record lands; FR-API-032 mint and FR-API-034 balance check keyed off the escrow token instead of `livemode`; Undecided 4 amended. Awaiting signature on the FR-API-032/034 amendment. |
| 2026-09-07 | William | Signed the FR-API-032/034 live-on-testnet amendment. |
| 2026-09-07 | Claude (for William) | Built the live-on-testnet amendment: `config.chains.live` defaults to 10143 (`LIVE_CHAIN_ID` overrides); `isMintable(chainId, token)` in `deployments.ts` keys the FR-API-032 mint and the FR-API-034 refusal off the escrow token; ingest's FR-API-072 guard compares the log's chain with the Subscription's chain instead of assuming live means 143; `registerDeployment` test seam stands in for the missing chain-143 record. `checkout-start.test.ts` (live start on testnet mints; AUSD chain refuses) and `ingest.test.ts` updated. 288 pass. |
| 2026-09-07 | Claude (for William) | Forms hardening ([ADR 2026-09-07 forms hardening](../decisions/2026-09-07-forms-hardening-logo-png-revoke-confirm.md)): FR-API-104 amended to PNG-only ≤ 50 KB served from a dashboard route; FR-API-065 endpoint URL cap; FR-API-113 CSV formula guard; FR-API-103 support URL scheme pinned and nullable. Awaiting signature. |
| 2026-09-07 | William | Signed FR-API-065/104/113 and the FR-API-103 amendment. |
| 2026-09-07 | Claude (for William) | Built FR-API-104 (`routes/dashboard-logo.ts`: multipart upload with PNG signature check and 50 KB cap, public hashed-URL serve with immutable caching and nosniff, delete; migration 0014 `merchants.logo_png`), FR-API-065 (endpoint URL ≤ 2048 on create and update), FR-API-113 (`lib/csv.ts` formula guard on the ledger export, amounts exempt), FR-API-103 amendment (`support_url` and `branding.support_url` http(s) only). 296 API tests pass. |
| 2026-09-07 | Claude (for William) | `/account` on real data ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)): FR-API-121 amended (identity token auth, both modes with `livemode`, receipt fields), FR-API-122 deferred, FR-API-123 amended (account cancel prepare + cancel, receipt email with rate limit). Awaiting signature. |
| 2026-09-07 | William | Signed the FR-API-121/123 amendments. |
| 2026-09-07 | Claude (for William) | Built FR-API-121/123 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)): `routes/account.ts` + `db/account.ts` (list across merchants by the token's wallet, both modes, `livemode` and receipt figures, no `pending_tx` on the wire since the hash is chain detail; cancel prepare + cancel through the checkout service; receipt email through the mailer, one per subscription per 10 min via `receipt_emailed_at`, migration 0015 with the `customers(wallet_address)` index). Account routes share the checkout CORS rule. `test/account.test.ts`; 301 API tests pass. |
| 2026-09-07 | Claude (for William) | FR-API-126 added ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md)): subscriber-initiated follow-on session. Awaiting signature. |
| 2026-09-07 | William | Signed FR-API-126. |
| 2026-09-07 | Claude (for William) | Built FR-API-126 ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md)): `POST /v1/checkout/sessions/:id/again` on the session router (identity token; 403 mismatch, 409 `not_ended`, 400 `product_archived`, 429 after 10 per session per hour), migration 0016 (`last_max_duration_seconds`, `again_of`), the public projection carries `last_max_duration_seconds`, audit `checkout_session.again`. `test/checkout-again.test.ts`; 303 API tests pass. |
| 2026-09-07 | Claude (for William) | FR-API-121 amended: rows carry `checkout_session` for Start again from the account page (William asked for the button there too). Awaiting signature. |
| 2026-09-07 | William | Signed the FR-API-121 `checkout_session` amendment. |
| 2026-09-07 | Claude (for William) | Built the FR-API-121 `checkout_session` amendment (account rows carry the `cs_` id). |
| 2026-09-07 | Claude (for William) | FR-API-123 receipt email hardened after a live Resend refusal: a failed send releases the 10-minute slot and answers `502 receipt_email_failed` ("We couldn't send the receipt right now. Try again in a moment."); the provider's reason is logged, never the address. Resend's test sender delivers only to the account's own email; production needs the domain verified. |
| 2026-09-07 | Claude (for William) | FR-API-126 amended: one follow-on per session, `restarted_as` on the projections. Awaiting signature. |
| 2026-09-07 | William | Signed the FR-API-126 `restarted_as` amendment. |
| 2026-09-07 | Claude (for William) | Built the FR-API-126 `restarted_as` amendment: session rows compute the newest follow-on; the public projection and account rows carry it; a second `again` → `409 already_restarted` naming the follow-on (archived product still wins with 400). 304 API tests pass. |
| 2026-09-07 | Claude (for William) | FR-API-044..047 added and FR-API-043 superseded from [ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md): subscriber pause/resume as signed relays under the session and the account, `allow_pause` on account rows, 10/h per subscription. Awaiting signature. |
| 2026-09-07 | William | Signed FR-API-044..047 and the FR-API-043 supersession. |
| 2026-09-07 | Claude (for William) | Built FR-API-044..047: `prepareRelay`/`submitRelay` in the checkout service generalise cancel over the action with tagged digests (`relay-auth`), `readRelayNonce` + `pauseFor`/`resumeFor` on the relayer, four session routes and four account routes (hidden), `product.allow_pause` on account rows, audit rows `subscription.pause`/`subscription.resume` counted for the 10/h limit, `429 rate_limit_error code=rate_limited`. `test/pause.test.ts` (6). Test fake chain now hashes in its own `0xfa…` space so fixture logs never collide with a start tx. 310 tests green. Live against the chain only after the FR-CON-018 redeploy. |
| 2026-09-07 | Claude (for William) | FR-API-048 balance read added; FR-API-034 amended: live mode on testnet escrows AUSD ([ADR 2026-09-07 add funds](../decisions/2026-09-07-add-money-and-ausd-live-on-testnet.md)). Awaiting signature. |
| 2026-09-07 | William | Signed FR-API-048 and the FR-API-034 AUSD amendment. |
| 2026-09-07 | Claude (for William) | Built FR-API-048 and the FR-API-034 amendment: `escrowTokenFor(chainId, livemode)` (AUSD in live mode, MockUSD in test, AUSD everywhere on 143), `readCheckoutBalance` + hidden `GET /checkout/sessions/:id/balance`, live pk fixture. `test/checkout-balance.test.ts` (3); 313 API tests green. |
| 2026-09-08 | Claude (for William) | FR-API-075 relayer runway block on `GET /v1/status` added from the grill (threshold on hours left, address shown, worker samples the chain). **Awaiting William's signature.** |
| 2026-09-08 | William | Signed FR-API-075. |
| 2026-09-09 | Claude (for William) | FR-API-040 gains `manage_url` ([ADR 2026-09-09 manage_url](../decisions/2026-09-09-subscription-manage-url.md)): merchants cannot pause on a subscriber's behalf, so the object carries the hosted session page link. **Awaiting William's signature.** |
| 2026-09-09 | William | Signed the FR-API-040 `manage_url` amendment. |
| 2026-09-09 | Claude (for William) | FR-API-107 was not built to FR-API-080: `GET /v1/dashboard/ledger` returned `{data}` with `limit` up to 1000 and no cursor. It now returns `{object:"list", data, has_more, url}` with `?starting_after=<ledger id>` and `limit` 1–100 (default 10), ordered by `seq` desc; filters compose with the cursor; `?format=csv` unchanged. Bug fix against the signed row, no new signature. |
| 2026-09-09 | Claude (for William) | FR-API-052 amended for FR-DSH-126 (William, yes in session): `GET /v1/invoices` gains `since`/`until` (unix, on `period_end`) and `status=paid\|failed`, composing with the cursor, so the dashboard's date and status filters run server-side across pages instead of over one fetched page. SDK method list unchanged. |
| 2026-09-09 | Claude (for William) | FR-API-011 amended for FR-DSH-126 (William, yes in session): `GET /v1/products?active=true\|false`, Stripe's filter, composing with the cursor; SDK `products.list({ active })`. The dashboard's "show archived" now runs server-side across pages. |
| 2026-09-09 | Claude (for William) | Built the rest of FR-API-109 (William: "build it"): `first_delivery_succeeded` on the first 2xx per mode, `key_expiring` and `secret_expiring` from the worker sweep (FR-WRK-042), and the emails for `endpoint_exhausted` and the two expiry kinds behind the merchant switches, `emailed_at` stamped on send. Migration 0018 adds `notifications.dedupe_key` (unique per merchant) so a restart never writes a notice twice. Mail body is `noticeMail` on the sign-in mail's shell; never carries a key or secret. |
| 2026-09-09 | William | Signed FR-API-135 (dashboard search; ADR 2026-09-09). |
| 2026-09-09 | Claude (for William) | Built FR-API-135: `searchObjects` is one UNION over the six tables, id prefix by `left()` (never LIKE, every id has an underscore), names and emails by `position()` case-insensitively, ordered by type then newest, `LIMIT 5`. Hidden from the public OpenAPI. Tests in `dashboard-search.test.ts`. |
| 2026-09-09 | Claude (for William) | Receipt email (FR-API-123) moved onto the shared mail shell (William: every mail follows the template): `receiptMail` in `mail-templates.ts` with the figures as rows, "Manage your meters" button to `/account`, merchant support link in the footer; text part unchanged in content. All three senders (sign-in, notices, receipt) now use one layout. |
| 2026-09-09 | Claude (for William) | FR-API-136 dashboard-only `context` on events (dashboard FR-DSH-093). Awaiting William's signature. |
| 2026-09-09 | William | Signed FR-API-136. |
| 2026-09-09 | Claude (for William) | Built FR-API-136: one subquery on the events read columns, joined through the subscription named by the payload (`data.object.subscription`, or the object id for subscription events) within the row's merchant and mode; `amount_settled` copied from invoice payloads. Attached only when `auth.via === "session"`, and on the overview's recent events. Additive `customer` id in the shape, for FR-DSH-093's no-email fallback. `openapi.json` unchanged. |
| 2026-09-13 | Claude (for William) | [ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md) applied: FR-API-032 mint clause deleted, FR-API-034 refusal in both modes, FR-API-048 `needs_funding` true in both modes with the AUSD label, Undecided 4 note. MockUSD leaves the runtime: relayer `mintMock`, `isMintable` and the `mockUsd` deployment field go. **Awaiting signature.** |
| 2026-09-13 | William | Signed the AUSD-only amendments. |
| 2026-09-14 | Claude (for Furqaan) | **Merchant-started metering, awaiting sign-off.** FR-API-049 `POST /v1/subscriptions/:id/start` (relayer submits `start()` as keeper); FR-API-127 expiry for authorised-but-unstarted sessions; FR-API-033 session completion moves off `StreamStarted` onto the funding event, else a completed checkout stays `open` and can be prepared again; FR-API-071 splits create from start, with `incomplete → active` carried by `subscription.updated` so the frozen six event types are untouched. Depends on contracts FR-CON-019/055. |
| 2026-09-14 | Furqaan | **Signed** FR-API-049/127 and the FR-API-033/071 amendments, including the recommended expiry window `min(max_duration_seconds, 15 min)`. |
| 2026-09-14 | Claude (for Furqaan) | **Start mode lives on the Product** (Furqaan chose option B, 2026-09-14). `products.start_mode` (`checkout` default, or `merchant`) says when a meter begins, and it is **snapshotted onto the subscription at prepare** so a running meter keeps the mode it started under even if the product changes later — the same reasoning that snapshots `feeBps` into a clone. `startSession` passes `noStart` to the factory for a `merchant` product. Migration `0019_start_mode` adds both columns plus a partial index for the FR-WRK-075 sweep. **Not in the signed text:** the schema change and the `products.create` body field were not specced; recorded here for review. |
| 2026-09-14 | Claude (for Furqaan) | **In-flight start guard.** FR-API-049 says a second call → `409`, but a subscription stays `incomplete` until `StreamStarted` ingests, so a status guard alone lets a second call inside that window submit a *second* relayer transaction — the contract reverts `InvalidState`, so nothing double-bills, but gas is burnt and the caller gets a `202` that did nothing. `pending_tx` cannot serve as the guard: `createWithPermitNoStart` already set it on every authorised-but-unstarted row, so guarding on it would reject the first legitimate start. Migration `0020_start_submitted` adds `subscriptions.start_submitted_at`, stamped when the relayer accepts the start; a row carrying it → `409 already_started`. **Not in the signed text:** recorded here for review. |
| 2026-09-15 | Claude (for Furqaan) | **Subscriber side of merchant-started metering, awaiting sign-off** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). FR-API-137 puts `start_mode` and a server-computed `start_by` on the public session's subscription and lets the checkout's signed cancel run on a funded unstarted stream; FR-API-138 lists those rows on `/account` and lets its cancel run on them. Both stamp `cancel_submitted_at`, so the FR-WRK-075 sweep never resubmits a cancel the subscriber already sent (without it the sweep's cancel would revert and burn gas). `start_by` comes from the sweep's own function, so the 15-minute window is defined once. |
| 2026-09-16 | Furqaan | **Signed** FR-API-137/138. |
| 2026-09-16 | Claude (for Furqaan) | **Built** FR-API-137/138 (`api/test/held-session.test.ts`, 4 tests). `startBy()` lives beside the FR-WRK-075 sweep in `worker/unstarted.ts` and reads the same `UNSTARTED_WINDOW_S`; the sweep's SQL applies the identical `LEAST(max_duration_seconds, window)`. The relayed cancel accepts a held subscription (`isHeld`) and stamps `cancel_submitted_at`. `/account` lists held rows in its unfiltered response only: the `status` filter keeps FR-API-121's three values, so `?status=incomplete` is still `400`. The `Subscription` component in `openapi.json` drops a stray `nullable: true` that the hidden public route had leaked into it. |
| 2026-09-16 | Claude (for Furqaan) | **Not in the signed text**, recorded for review: the public checkout-session projection's `product` now carries `start_mode`. Checkout FR-CHK-035 needs the mode on the cap step, which runs before `prepare`, when there is no subscription for FR-API-137's field to sit on. FR-API-031's exact-shape test lists the new field; the merchant-facing Product already returned it. |
| 2026-09-17 | Claude (for Furqaan) | **FR-API-139/140, awaiting sign-off.** FR-API-139 refuses subscriber stop and pause on a started merchant-mode meter with `409 merchant_controlled` ([ADR](../decisions/2026-09-17-merchant-started-meters-only-merchant-stops.md)); FR-API-140 removes `url`, repoints `manage_url` to `/account`, opens public session reads to any origin, and ties the popup to the `success_url` origin ([ADR](../decisions/2026-09-17-react-sdk-replaces-hosted-checkout.md)). |
| 2026-09-17 | Furqaan | **Signed** FR-API-139/140 and the FR-API-030/040 amendments (merchant stops; no hosted checkout). |

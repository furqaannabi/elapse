# Hosted checkout (`/c/[session]`) — FRD

Status: **FR-CHK-033–036 and the FR-CHK-032 amendment (merchant-started metering) Signed 2026-09-16 (Furqaan)** · **Signed 2026-09-03 (William); FR-CHK-027 signed 2026-09-07 (William); FR-CHK-028 signed 2026-09-07 (William); FR-CHK-018/020/025 amendments and FR-CHK-029 signed 2026-09-07 (William); FR-CHK-007 start-again amendment signed 2026-09-07 (William); FR-CHK-020 Start-again-on-account amendment signed 2026-09-07 (William); FR-CHK-007 one-follow-on amendment signed 2026-09-07 (William); FR-CHK-030 and the FR-CHK-005/018 Stop amendments signed 2026-09-07 (William); FR-CHK-031 Add funds signed 2026-09-07 (William)** — approved after review; built against the mock API. Decisions taken: Pause behind `allow_pause` default off; duration presets with the dollar ceiling shown (FR-CHK-003, revised 2026-09-04 from dollar presets); low-balance at 5 min; email receipt mocked; judge mode via `?judge=1` and triple-tap. May be revisited. **Surface 4 (`/account`, FR-CHK-016–026) added and signed 2026-09-04 (William).** · Surface: Operate (subscriber, mobile-first) · Sources: design brief Surface 2; detailed doc §3, §7, §10.

## Problem

A subscriber is sent a link by a merchant. On a phone, with no account and no crypto knowledge, they must understand the price, start, watch a meter, cancel when they want, and see exactly what they paid. The judges watch this on a phone in the demo video.

## User stories

1. As a subscriber, I want to see who is charging me and at what rate per second (and per hour), so that the price reads like a price.
2. As a subscriber, I want to sign in with Face ID, so that I never see a wallet.
3. As a subscriber, I want to add funds with a preset, so that I know my maximum exposure before starting.
4. As a subscriber, I want a live counter and one Cancel button, so that stopping is instant and obvious.
5. As a subscriber, I want a receipt that says what I paid and what was returned, so that I trust the meter.
6. As a merchant, I want the subscriber redirected to my success URL with the session id, so that I can provision access.
7. As a judge, I want a hidden technical panel, so that I can verify the chain without it polluting the consumer UI.

## Functional requirements

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-CHK-001 | The page loads a checkout session by id and shows merchant name/logo, product name, rate per second, and derived per-minute and per-hour figures. | Renders from mocked `GET /v1/checkout/sessions/:id`. |
| FR-CHK-002 | Sign in with Face ID / passkey (Privy) with an email fallback. Until Week 3 the wallet layer is a mock behind the same interface. | `AuthProvider` interface; mock resolves after a confirm sheet. |
| FR-CHK-003 | Cap step (replaces the fund step, [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md)): presets **1 hour / 4 hours / custom** in runtime, each showing the dollar maximum it means at the rate ("Up to $14.40"), plus "You only pay the seconds you use. Anything unused comes back when you cancel." Start asks for one Face ID confirmation, which signs a permit for exactly that maximum; no separate funding action, no Add funds anywhere. The wallet already holds the subscriber's dollars (BR-CHK-007); a wallet with less than the chosen cap shows "You have $X available" and offers the largest preset it covers. **Amended 2026-09-07** ([ADR 2026-09-07 add funds](../decisions/2026-09-07-add-money-and-ausd-live-on-testnet.md)): on real sessions the balance comes from FR-API-048 once signed in; when no preset and no custom length is affordable, Continue is replaced by **Add funds**, which opens FR-CHK-031. The line is hidden while the balance is unknown (test mode never needs it: the token is minted at Start). **Amended 2026-09-13** ([ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md)): minting is gone, so the balance line and the Add funds path run identically in both modes; test mode stops being exempt. Copy never says permit, approve, allowance, or token. | Selecting 1 hour at $0.004/s shows "Up to $14.40"; Start triggers one mock confirm; an insufficient wallet disables the preset with the available line. |
| FR-CHK-004 | Start opens a subscription (`incomplete → active`), records `started_at`, and switches to the meter view on the same URL. | State machine test. |
| FR-CHK-005 | Meter view: full-screen `Readout` (hero), rate reminder, "Started h:mm", a primary "Back to {merchant}" that returns the subscriber to the merchant's product with the meter still running (FR-CHK-009), and ~~Cancel~~ **Stop** as a neutral outline beneath it (**amended 2026-09-07**, [ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md): the subscriber side says Stop everywhere, as `/account` already does; the dashboard keeps Cancel and the landing hook keeps "Cancel at 83 seconds"; the cap step's "comes back when you cancel" becomes "when you stop"); optional Pause if the product allows it (FR-CHK-030). Returning is the expected path — the subscriber came to buy seconds of the merchant's product, not to watch our counter — so the copy under the buttons reads "Your meter keeps running. Stop it here or from your meters at any time." with a link to `/account`. | Component test; tick at 100 ms; the return link carries the session id. |
| FR-CHK-006 | Low-balance state: when remaining runtime < 5 min, an amber notice "About N minutes left of your {cap}" naming the cap the subscriber chose (“your 1 hour”). No Add funds: the cap is fixed for the session (FR-CHK-007). | Threshold test at rate × cap. |
| FR-CHK-007 | Reaching the cap ends the session (decided 2026-09-04, William; [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md) — the cap is the pot and cannot be raised mid-session). At the capped second the meter stops and the receipt appears exactly as after a cancel (FR-CHK-008) with one extra line, "Your 1 hour is up", and a "Start again" button that opens a fresh checkout session for the same product. Status is `canceled`, never `paused`; there is no "Add funds to resume" and no out-of-funds pause state. The merchant is told through `invoice.payment_failed` and `subscription.canceled` (API FR-API-051); the subscriber sees neither word. **Amended 2026-09-07 ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md)):** "Start again" appears on every receipt, after a cancel as well as at the cap (the cap line stays cap-only). It calls FR-API-126 with the identity token, then the page navigates to the new session's URL, whose cap step opens with the last cap preselected. Busy state "Opening…"; an archived product answers "This product is no longer available." on the receipt. **Amended 2026-09-07 (William):** a receipt whose session has a follow-on shows "A newer session followed this one · open it" linking to it instead of the button; only the newest receipt in a chain offers Start again. Same on the account receipt sheet (FR-CHK-020). | State test: elapsed reaches cap → receipt with the cap line and Start again; no Add funds control exists in any state. |
| FR-CHK-008 | Cancel ends the meter, shows the receipt: "You paid for N seconds · $X" hero line; breakdown of started, canceled, rate, total, refunded; "Back to {merchant}" and "Email receipt". | Settled = whole seconds × rate; refund = funded − settled. |
| FR-CHK-009 | "Back to {merchant}" navigates to `success_url?session_id=cs_…` from the running meter (FR-CHK-005) and from the receipt (FR-CHK-008). Abandoning before Start goes to `cancel_url`. The merchant's server has already had `checkout.session.completed` when the subscriber lands, so it can provision access and draw its own meter from `rate_usd_per_second` and `started_at` (SDK `subscriptions.retrieve`) with its own stop button (`subscriptions.cancel`). | URL assertions from both states. |
| FR-CHK-010 | Session states: loading skeleton, expired, already used, product archived, network error — each with copy naming the problem and the recovery. | One test per state. |
| FR-CHK-011 | Judge mode: hidden toggle (`?judge=1` or triple-tap on the footer) slides up a panel with chain id, contract address, block ticker (~300 ms), indexer status, and the live webhook delivery log for this session. | Panel renders from mocked data; hidden by default. |
| FR-CHK-012 | Mobile 390 first; one-handed; touch targets ≥ 44 px; Cancel reachable with the thumb. | Screenshot review at 390 and 1440. |
| FR-CHK-013 | Light and dark; reduced motion honoured; counter keeps ticking. | As landing. |
| FR-CHK-014 | Merchant branding renders from the session: business name, logo, accent colour (falls back to the default amber), support/terms link. Layout and copy are never merchant-controlled. | Session with branding shows logo + name + accent; session without shows defaults. |
| FR-CHK-015 | Until the API exists, the page runs against an in-memory mock that seeds sessions for every state (open, running, low balance, ended at the cap, canceled, expired, already used, archived) so each screen is reachable by URL. | `/c/cs_demo`, `/c/cs_expired`, `/c/cs_used`, `/c/cs_archived`, `/c/cs_lowbal`, `/c/cs_capped` render their states. |


## Surface 4 — Subscriber account (`/account`)

Decided 2026-09-04 (grill round, William): specced here per dashboard decision 16; built after the dashboard, first cut if the deadline bites. Same rules as the checkout: no chain words, no red, mobile first. The grill's funds answer (an Elapse balance) was overtaken the same day by [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md): the subscriber's own wallet holds the money, so FR-CHK-021 is withdrawn and the page has no funds block.

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-CHK-016 | Sign-in is the same passkey / Face ID identity the subscriber created at their first checkout (Privy), with the same email fallback as FR-CHK-002 and no separate credential. Signed-out `/account` shows one sentence and a Sign in button; after sign-in the page loads in place. | `AuthProvider` reused; signed-out state test; mock resolves after the confirm sheet. |
| FR-CHK-017 | Entry points: a "Manage your meters" link in the checkout footer once signed in, on the receipt (FR-CHK-008), and in the email receipt. No other navigation to `/account` exists. | Link present in meter and receipt states, absent before sign-in; email template carries the URL. |
| FR-CHK-018 | Running meters: a live total above them ("2 meters running · $2.601 · $17.64 / hour while they run") so the answer to "what am I paying right now" needs no scrolling, then a list across merchants of every `active` or `paused` subscription for this identity (API FR-API-121). Each row is compact — merchant mark and name with the product beside it, an inline `Readout` ticking at 100 ms from `rate × (now − started_at)`, the cap line from FR-CHK-021 with remaining runtime, and Stop — so six running meters still read on one phone screen. Past sessions show three, then "Show 3 more". Sorted by started time, newest first. Low-balance rows reuse FR-CHK-006 copy; a meter that reached its cap has already moved to the receipts list (FR-CHK-007). **Amended 2026-09-07 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)):** rows come from `GET /v1/account/subscriptions` with the identity token; test-mode rows carry a small "Test" tag, the only mode word on the page; the page polls the list every 5 s while a meter runs or a cancel is pending, and a row whose cancel is pending shows "Stopping…" until ingest confirms. **Amended 2026-09-07** ([ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md)): a row whose product allows pause shows Pause beside Stop while `active`, and Resume beside Stop while `paused`, one tap each with no sheet (FR-CHK-030); the row's `Readout` is frozen while paused; a pending pause or resume shows "Pausing…" / "Resuming…" until ingest confirms. | Two merchants in the mock render two rows; ticker math shares `lib/meter/math`. |
| FR-CHK-019 | Cancel from the list opens a confirm sheet: "Stop the meter at {merchant}?" with "You'll pay N seconds so far · $X" live, a neutral Cancel meter button and a Keep running button. Confirming calls the same cancel path as the checkout; the row becomes its receipt line in place. Checkout's own Cancel stays one tap (FR-CHK-008). | Sheet names the merchant; confirm produces the receipt; dismiss leaves the meter running. |
| FR-CHK-020 | Receipts: every settled or canceled subscription, newest first, one line each: merchant, "You paid for N seconds · $X", date. Tapping opens the FR-CHK-008 receipt with "Email receipt". Per-second detail only; never a fee line (subscribers pay gross). **Amended 2026-09-07 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)):** receipts are the caller's `canceled` subscriptions (`seconds_elapsed`, `settled_usd`, `refunded_usd`, `ended_reason`), one line per ended meter; the invoices route is not used. **Amended 2026-09-07 ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md), William):** the account receipt sheet offers Start again, calling FR-API-126 for the receipt's session and navigating to the new session's URL; the same archived-product message applies. | List from mock invoices; detail matches the checkout receipt component. |
| FR-CHK-021 | Withdrawn 2026-09-04 ([ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md)): no Elapse balance, no card, no Return to card. The account page shows no funds block. Each meter row shows its cap and how much of it is used ("$0.33 of $14.40"), read from the subscription; unused escrow returns to the subscriber's wallet on cancel and the receipt says "$14.07 returned". | Row renders used/cap; no balance component exists on the route. |
| FR-CHK-022 | Identity: meters are grouped by the passkey wallet address (API FR-API-121). The address itself is never displayed. No new object or id prefix. | Render test asserts no `0x` string. |
| FR-CHK-023 | Empty state: "No meters yet. When a merchant sends you a link, your meters show up here." with nothing else. | Empty mock renders the sentence only. |
| FR-CHK-024 | Mobile 390 first, one hand, ≥ 44 px targets; the list is a card stack, never a table. One column on phones and tablets, two from `lg` where the container widens to exactly two cards (888 px): a narrower cell truncates the amount, which is the one figure that may never be cut. Light and dark; reduced motion honoured; tickers keep ticking. | Screenshot review at 390 and 1440. |
| FR-CHK-025 | Rewritten 2026-09-07 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)): the route reads the real API through the `AccountApi` interface; `?as=` seeds are gone from the route. The in-memory mock survives only behind that interface for component tests (`two-merchants`, `empty`, `low-balance`, `signed-out`). A visitor can never reach invented data. | Route test: `?as=two-merchants` renders the real client's view, not the seed. |
| FR-CHK-026 | No judge mode on `/account` for 13 Oct; the panel lives on the checkout only (FR-CHK-011). | No `?judge=1` handling on the route. |

| FR-CHK-027 | Identity proof ([ADR 2026-09-07 identity token](../decisions/2026-09-07-privy-identity-token-on-prepare.md)): the Privy provider enables identity tokens; before `prepare` and before `cancel/prepare` the page fetches a fresh identity token from Privy and sends it as `X-Privy-Token`. It no longer sends `wallet_address` or `email`; both come from the token on the API side (FR-API-120). On `401 subscriber_auth_invalid` the page fetches a new token and retries once, silently; a second 401, or `403 subscriber_mismatch`, shows "Sign in again" with the FR-CHK-002 sign-in sheet, and a `503 subscriber_auth_unconfigured` shows "Checkout is not set up yet." No chain words in any of these states. | Mocked API: retry once on the first 401; sign-in sheet on the second; sheet on 403; sentence on 503; the request body carries no wallet or email. |
| FR-CHK-029 | Email receipt ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)): "Email receipt" on the checkout receipt (FR-CHK-008) and the account receipt (FR-CHK-020) calls `POST /v1/account/subscriptions/:id/receipt/email` with the identity token; the button reads "Sent to {email}" for 10 s after success and is disabled while pending; the API's rate limit (one per receipt per 10 min) surfaces as "Already sent. Check your inbox.". No email on the identity → the button is absent. | Receipt tests: sends once, shows the sent state, shows the limit copy on 429, absent without an email. |
| FR-CHK-030 | **Pause and resume on real sessions** ([ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md)): when `product.allow_pause` is true the meter view offers Pause while running (FR-CHK-005) and Resume while paused; each is one tap with no confirmation because nothing is charged or refunded and the action reverses. The page calls `pause/prepare` → the subscriber signs the message with the embedded wallet → `pause {signature, deadline}` (API FR-API-044), then polls until `paused`; resume mirrors it (FR-API-045). While paused the `Readout` is frozen at the pause instant, the status line reads "Paused", and the copy under the buttons reads "Nothing is charged while paused. Resume when you're ready, or stop to get the rest back." Stop stays available while paused (FR-CON-024: paid to the pause instant, the rest returned). A `429` from FR-API-047 reads "Too many changes. Try again in a bit." The Pause button never appears when the API cannot honour it: `allow_pause: false` products show Stop only. Nothing on this surface names a signature, a transaction, or a wallet (BR-CHK-001). | Component test with `allowPause: true`: Pause → status Paused, Readout constant across advanced timers, Resume → ticking again with the paused span excluded; `allowPause: false` → no Pause button; real-api test: prepare, sign, submit, poll; 429 copy. |
| FR-CHK-031 | **Add funds** ([ADR 2026-09-07 add funds](../decisions/2026-09-07-add-money-and-ausd-live-on-testnet.md)): a full-height step reached from the cap step (nothing affordable) or from a Start refused for funds (FR-API-034). Heading "Add funds to start". Shows **Balance $X** (ticking up as money lands), **Needed $Y** for the chosen or smallest cap, a QR code of the receiving address, the address in full with a Copy button ("Copied"), and the one sentence "Send AUSD on Monad to this address. It usually arrives within a minute." Polls FR-API-048 every 5 s; when the balance covers the cap it toasts "Funds arrived" and returns to the cap step with the balance line updated. A "Not now" link goes to the merchant's cancel URL. This is the one subscriber screen besides judge mode that names the token and the network (BR-CHK-001 exception); it never shows a token contract, an explorer link, a fee, a seed or a key. Seeded demo: `/c/cs_short` — a wallet holding $0.50 that becomes $20 after 6 s. **Amended 2026-09-13** ([ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md)): the step runs in **both** modes — `needs_funding` is always `true` (FR-API-048) and a test-mode wallet short of the cap reaches this screen exactly as a live one; copy, states and the BR-CHK-001 exception are unchanged. | Component tests: shows balance, needed, address, copy; poll advances to the cap step when the balance covers it; no other chain words; 390 px. Real-api test: balance route called with the identity token; `needs_funding: false` skips the screen. |
| FR-CHK-032 | **The meter follows the server** (judge pass 2026-09-09): while a real session's subscription is `active` or `paused`, the meter view re-reads the session every 5 s and whenever the tab regains focus. A status that comes back `canceled` (merchant cancel via FR-API-042, or the cap ending on chain) moves the page to the receipt exactly as after Stop, with the server's totals (BR-CHK-003). A `paused`/`active` change from elsewhere (the account page in another tab) updates the readout. Polling stops when the page leaves the meter view. **Amended 2026-09-15 (awaiting sign-off, [ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)):** the held view (FR-CHK-034) follows the server the same way, so a merchant's start moves the page to the meter, and a refund to the receipt, without a reload. | Fake timers: advance 5 s, the mocked session flips to canceled → receipt with the server's seconds; a focus event triggers one read; no requests once the receipt is shown. |
| FR-CHK-033 | **Authorised, not started** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). For a session whose subscription has `start_mode: "merchant"`, Face ID does not lead to the meter. The page shows "Authorised — taking you back to {merchant}" and re-reads the session every 2 s until it is `complete` (API FR-API-033: the funding confirmed), then navigates to `success_url?session_id=cs_…` without a tap (FR-CHK-009). If the session is not `complete` within 20 s, it stops waiting and shows "Back to {merchant}" instead, so nobody is stuck behind a slow confirmation. No meter, no ticker. | Fake timers: `complete` on the third read → one navigation to the success URL carrying the session id; never `complete` → at 20 s the button appears and no navigation happens. |
| FR-CHK-034 | **Held view** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). When a merchant-mode subscription is `incomplete` and funded, opening its session (manage link, `/account`, a reload) shows the held view, never the Start step: "Waiting for {merchant} to start", "$X held · You haven't been charged.", and "If it hasn't started by h:mm, it all comes back to you." (`start_by` from API FR-API-137, in the subscriber's local time, the same format as "Started h:mm"). A primary "Back to {merchant}" and Stop as the neutral outline beneath. Stop runs the checkout's existing signed cancel and lands on the receipt, which reads "You paid for 0 seconds · $0.00" with the whole amount refunded (FR-CHK-008). The view follows the server (FR-CHK-032): `active` turns it into the meter view, `canceled` into the receipt. | View test: `incomplete` + `merchant` + funded → `held`, never `ready`. Component test for the copy and both buttons. Mocked flip to `active` → meter view; Stop → receipt with 0 seconds and the full refund. |
| FR-CHK-035 | **The cap step says when billing starts** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). For a merchant-mode product the cap step adds one line under "You only pay the seconds you use…": "Billing starts when {merchant} starts your session." Checkout-mode products are unchanged. | Snapshot for both modes. |
| FR-CHK-036 | **`/account` shows held money** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). Funded, unstarted merchant-mode subscriptions (API FR-API-138) appear above running meters as compact rows: merchant and product, "Not started · $X held", "Comes back at h:mm if not started", and Stop, which opens FR-CHK-019's confirm sheet with "You haven't been charged" in place of the live cost. They are left out of the running total (FR-CHK-018) because nothing is accruing. When the merchant starts, the row becomes a running meter on the next read; when refunded, a receipt. | Account page test: a held row renders with no `Readout`, is excluded from `RunningTotal`, and its Stop sheet shows no cost line. |
| FR-CHK-028 | Subscriber inputs check what the server checks ([ADR 2026-09-07 forms hardening](../decisions/2026-09-07-forms-hardening-logo-png-revoke-confirm.md)): email needs one `@` with a dot after it, ≤ 254, `autoComplete="email"`; the one-time code is six digits (`maxLength=6`, `inputMode="numeric"`, `pattern="[0-9]*"`, `autoComplete="one-time-code"`), Continue disabled until six digits; the custom cap accepts whole minutes between 1 and 43 200 (60 s–30 d, FR-API-032's bounds) with the hint "Between 1 minute and 30 days" on an out-of-range value; every field is trimmed before use. | Sheet tests: `a@b` disabled, `a@b.co` enabled; `12345` disabled, `123456` enabled, letters rejected; cap `0` and `43201` disabled with the hint. |

Business rule: BR-CHK-008 — `/account` inherits BR-CHK-001 to BR-CHK-007 unchanged.

Account data (mocked until the API exists):

```
AccountMeter    { subscription: sub_…, merchant: { name, logo_url, support_url }, product: { name, rate_usd_per_second },
                  status, started_at, paused_at, funded_usd, settled_usd }
AccountReceipt  { invoice: in_…, merchant: { name }, seconds, amount_settled, settled_at, subscription: sub_… }
```

Grill-me questions (settled 2026-09-04): sign-in same passkey as checkout · entry via checkout links and the email receipt · contents are meters, receipts, and funds · funds shape was "one Elapse balance plus per-meter escrow", withdrawn the same day by [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md) · cancel through a confirm sheet naming the merchant · identity is the passkey wallet address, no new object.

## Business rules

| Id | Rule |
| --- | --- |
| BR-CHK-001 | No chain vocabulary anywhere on the subscriber surface except inside the judge panel: no wallet, gas, seed, 0x, Monad, connect, transaction. **Exception added 2026-09-07** ([ADR 2026-09-07 add funds](../decisions/2026-09-07-add-money-and-ausd-live-on-testnet.md)): the Add funds step (FR-CHK-031) names the token ("AUSD"), the network ("Monad") and shows the receiving address, because a person sending money must know what to send and where. Nothing else on it is chain vocabulary. |
| BR-CHK-002 | The subscriber can never be charged more than funded; the meter pauses at zero. |
| BR-CHK-003 | Settled amount is whole seconds × rate (matches the contract); the live counter may show fractional accrual but the receipt never exceeds settled. |
| BR-CHK-004 | No red on the subscriber surface (human's call, 2026-09-03). One accent: amber for the live meter, low-balance and out-of-funds notices (the latter with stronger copy and a filled notice, not a new colour). Cancel is a neutral outline button. |
| BR-CHK-005 | The merchant's secret key never reaches this page; it is driven by a session id and a publishable key only. |
| BR-CHK-006 | "Powered by Elapse" and a lock icon appear in the footer of every state. |
| BR-CHK-007 | The subscriber's money lives in their own wallet (the Privy embedded wallet holds AUSD, shown as dollars); Elapse never holds a balance for them and there is no card payment in the product ([ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md)). How a subscriber gets dollars into that wallet is outside Elapse for 13 Oct; the demo wallet is pre-funded on testnet. |

## Data (mocked until the API exists)

```
CheckoutSession { id: cs_…, merchant: { name, logo_url, success_url, cancel_url },
                  product: { id: prod_…, name, rate_usd_per_second, allow_pause },
                  status: "open" | "complete" | "expired", subscription?: sub_… }
Subscription    { id: sub_…, status, started_at, paused_at, canceled_at, funded_usd, rate_usd_per_second }
```

## Grill-me questions (settled 2026-09-03 with the recommendations)

1. Pause: in MVP or not? Doc lists `subscription.updated` for pause/resume but the checkout flow in §7 has no Pause. **Recommend: hide Pause behind `product.allow_pause`, default off.**
2. Fund presets: fixed dollars, or runtime-based ("1 hour", "4 hours")? **Recommend: dollars with the runtime shown beside each.**
3. Low-balance threshold: minutes or percent? **Recommend: 5 minutes of runtime.**
4. Email receipt: real send (needs backend) or copy-to-clipboard for MVP? **Recommend: button present, posts to a mocked endpoint, toast "Sent".**
5. Judge-mode trigger: `?judge=1` only, or also a gesture? **Recommend: both; the gesture is for the live demo on a phone.**

## Open

Parked 2026-09-06 (William: note and move to Week 4; none gates the demo):

- ~~**Subscriber pause on real sessions.** The contract's `pause()`/`resume()` are party-only with no relayed variant, so a subscriber would need MON for gas. A `pauseFor(deadline, signature)` in the shape of `cancelFor` would fix it but costs a redeploy. The demo product ships with `allow_pause` off; the page hides Pause on real sessions.~~ Decided 2026-09-07 (William): built as FR-CHK-030 on contracts FR-CON-018 ([ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md)). The demo product keeps `allow_pause` off.
- ~~**Email receipt** (FR-CHK-008) and **Start again** (FR-CHK-007) need API routes.~~ Both built 2026-09-07 (FR-API-123 receipt email, FR-API-126 again).
- **Hosted Envio deploy** (indexer FR-IDX-051): William connects the repo on Envio Cloud; `envio dev` covers every local run meanwhile.

- **Merchant-fixed cap is unusable on the page (found 2026-09-11, live-mode run; not fixed).** A session created with `max_duration_seconds` is rejected at `prepare` with `cap_fixed` for any cap the subscriber picks. The API is right; the client is wrong. `real-api.ts` types the session's `max_duration_seconds` but its mapper surfaces only `last_max_duration_seconds`, so `CapStep` never learns the cap is fixed and always renders a free picker. Fix is in the client: carry the field, and render a fixed cap as a stated duration instead of a picker. Not reachable from the dashboard (`createCheckoutLink` never sends the field and no UI sets one) and not used by `examples/saas`, so neither the demo nor the Week 6 clone-and-run gate is affected — but `checkout.mdx`, `sdks.mdx` and `testing.mdx` all teach the parameter, and `testing.mdx` recommends it, so a developer following the docs gets a checkout nobody can complete.

- Privy app id and bounty requirements (Week 3).
- `/account` is built after the dashboard and is the first cut if the deadline bites (dashboard decision 16).

## Revision

| Date | Who | Change |
| --- | --- | --- |
| 2026-09-03 | Claude (for William) | First draft from the detailed doc and design brief. |
| 2026-09-03 | William | Reviewed and signed; five open questions settled with the agent's recommendations; FR-CHK-014/015 added. |
| 2026-09-04 | Claude (for William) | Surface 4 subscriber account from the 2026-09-04 grill round: FR-CHK-016–026, BR-CHK-007, account data shapes. FR-CHK-021 marked proposed pending the funding ADR. Awaiting signature. |
| 2026-09-04 | Claude (for William) | [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md) applied: FR-CHK-003 becomes the cap step with one Face ID signature; FR-CHK-021 withdrawn (no balance, no card); FR-CHK-018 cap line; BR-CHK-007 wallet rule; account balance shape removed; open items on out-of-funds and rebuilding the fund step. |
| 2026-09-04 | Claude (for William) | Cap end decided (William, 2026-09-04): FR-CHK-007 becomes "the session ends at the cap" with a receipt and Start again, replacing the out-of-funds pause; FR-CHK-006 drops Add funds; FR-CHK-015 seed renamed; FR-CHK-018 adjusted. |
| 2026-09-04 | Claude (for William) | Built the cap model in `web/`: cap step replaces the fund step, the meter ends at the cap with a receipt and Start again, no Add funds anywhere; `CapStep`, `MeterView` and `Receipt` gained component tests. 196 tests green. |
| 2026-09-04 | William | Reviewed and signed Surface 4 (`/account`, FR-CHK-016–026). |
| 2026-09-04 | Claude (for William) | Built Surface 4 in `web/`: `/account` with meters across merchants, a confirm sheet naming the merchant, receipts, the cap line, empty and signed-out states, and "Manage your meters" links from the checkout. Elapse-branded per the ADR; no judge mode. 21 new tests, 219 green. |
| 2026-09-04 | Claude (for William) | FR-CHK-005 and FR-CHK-009: the running meter returns the subscriber to the merchant's product with the meter still running, so the hosted page is a step and not a destination. Built in the same pass. |
| 2026-09-04 | Claude (for William) | FR-CHK-018 gains the live running total and a compact row after review: three tall cards pushed the newest meter off a phone screen. Receipts page after three. |
| 2026-09-04 | Claude (for William) | FR-CHK-024: two-column grid from `lg` at full card width, one column below. |
| 2026-09-05 | Claude (for William) | **Wired to the real API** (`web/src/lib/checkout/real-api.ts` behind the unchanged `CheckoutApi`; seeded ids stay on the mock, FR-CHK-015). The hosted page sends no key: the session id is the pass, accepted from the checkout origin only (William, option a; API FR-API-004 revision). `start`/`cancel` return once the chain confirms (client polls). Pause/resume, email receipt and Start again are not offered on real sessions until their API routes exist (no "coming soon"). Judge panel reads `GET /v1/status` and the session's delivery log. |
| 2026-09-06 | Claude (for William) | **FR-CHK-002 revised (William, option a):** Privy attaches a passkey to an account rather than creating one from it (`disallowed_login_method` on passkey sign-up), so a first visit signs in by **email code or Google** (Google added the same day at William's request, headless `useLoginWithOAuth`, redirect back to the same URL), then offers "Use Face ID next time" (link passkey). A device that did so leads with "Continue with Face ID" on later visits (`localStorage` flag). A Privy session that survives a reload signs the page in silently. The embedded wallet is created explicitly after a headless login. View rule: a `complete` session whose meter is `active`/`paused` is running, not "already used" (FR-API-033 completes the session at start). **First real run 2026-09-06**: William, Google sign-in, 1 h cap, stream at block 60180928, meter live on Monad testnet. |
| 2026-09-07 | Claude (for William) | FR-CHK-027 identity proof added from the grill ([ADR 2026-09-07 identity token](../decisions/2026-09-07-privy-identity-token-on-prepare.md)): fresh identity token per binding call, one silent retry, then the sign-in sheet. Awaiting signature. |
| 2026-09-07 | William | Signed FR-CHK-027. |
| 2026-09-07 | Claude (for William) | Built FR-CHK-027: `real-api.ts` binding calls send `X-Privy-Token` from `RealApiOptions.identityToken`, retry once on a stale token, map `403 subscriber_mismatch` to `sign_in_required` and `503` to `unconfigured`; the page's `afterError` reopens the sign-in sheet on `sign_in_required`; the Privy layer supplies `getAccessToken()` then `getIdentityToken()`. Tests in `real-api.test.ts` and `view.test.ts`; web 257 pass. **Needs from William:** identity tokens enabled in the Privy dashboard, and `PRIVY_APP_ID` + `PRIVY_VERIFICATION_KEY` in `api/.env`. |
| 2026-09-07 | Claude (for William) | FR-CHK-027 first real run hit Privy's "Too many requests": the SDK's `getIdentityToken()` calls Privy's user endpoint every time. The page now sends the token the SDK already holds (`useIdentityToken`) and asks Privy only when it is missing or within a minute of expiry (`privy/identity.ts`, 4 tests). The normal cap click makes no Privy request. |
| 2026-09-07 | Claude (for William) | FR-CHK-002 gap from the first end-to-end run: after a reload the server session already had a Customer, so the page showed the cap step before Privy had restored this device's wallet, and the first Continue said "Sign in first". `AuthFlow.ready` (Privy ready and, with a session, the wallet list loaded) now holds the primary action as "Checking your sign-in…", then shows sign-in if the device has no session or the step once the wallet is in hand; this also removes the email → Face ID → Continue flash on load. `actionGate` in `view.ts` (tested); web 264 pass. |
| 2026-09-07 | Claude (for William) | FR-CHK-028 added ([ADR 2026-09-07 forms hardening](../decisions/2026-09-07-forms-hardening-logo-png-revoke-confirm.md)): email, code and cap client checks mirror the server. Awaiting signature. |
| 2026-09-07 | William | Signed FR-CHK-028. |
| 2026-09-07 | Claude (for William) | Built FR-CHK-028: sign-in sheet email and code inputs carry the shared rules and attributes, Continue waits for them; custom cap is whole minutes between 1 and 43 200 with the hint; `CheckoutFrame` drops a malformed accent or a non-http support link and opens support with `rel="noopener noreferrer"`. Landing hero grid fixed for 390 px (the code block's natural width was stretching the column). |
| 2026-09-07 | Claude (for William) | `/account` on real data ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)): FR-CHK-018/020 amended (real list, Test tag, receipts from canceled subscriptions, Stopping… state), FR-CHK-025 rewritten (seeds gone from the route, mock for tests only), FR-CHK-029 added (email receipt on both receipts). Awaiting signature. |
| 2026-09-07 | William | Signed the FR-CHK-018/020/025 amendments and FR-CHK-029. |
| 2026-09-07 | Claude (for William) | Built the FR-CHK-018/020/025 amendments and FR-CHK-029 ([ADR 2026-09-07 account on real data](../decisions/2026-09-07-account-page-on-real-data.md)): `lib/account/real-api.ts` (same `AccountApi` shape; signed out on a missing or refused token; cancel = prepare, sign, submit, poll the list to `canceled`), `lib/account/client.ts` picks it when the API URL is set (else an empty mock), `account-route.tsx` wraps Privy and waits for the restored sign-in, no `?as=`; page polls every 5 s on the API; Test tag on meter and receipt rows; Email receipt on both receipts with "Sent to …" for 10 s, the limit copy on 429, absent without an email (the auth flow now exposes the identity's email). Checkout client's `emailReceipt` posts to the account route. 306 web tests pass. |
| 2026-09-07 | Claude (for William) | FR-CHK-007 amended ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md)): Start again on every receipt through FR-API-126; the cap step opens with the last cap preselected. Awaiting signature. |
| 2026-09-07 | William | Signed the FR-CHK-007 start-again amendment. |
| 2026-09-07 | Claude (for William) | Built the FR-CHK-007 amendment ([ADR 2026-09-07 start again](../decisions/2026-09-07-start-again-after-any-end.md)): real client `startAgain` posts to `again` and loads the new session; the receipt offers Start again after any end (the cap line stays cap-only); `CapStep` takes `initialSeconds` and preselects the preset or fills the custom minutes; the page passes the new session's last cap. 309 web tests pass. |
| 2026-09-07 | Claude (for William) | FR-CHK-020 amended: Start again on the account receipt sheet. Awaiting signature. |
| 2026-09-07 | William | Signed the FR-CHK-020 Start-again-on-account amendment. |
| 2026-09-07 | Claude (for William) | Built the FR-CHK-020 amendment: the account receipt sheet offers Start again (`AccountApi.startAgain` → FR-API-126 → full navigation to the new session's URL). 310 web tests pass. |
| 2026-09-07 | Claude (for William) | FR-CHK-007 amended: a restarted receipt shows "Started again" with a link, no button. Awaiting signature. |
| 2026-09-07 | William | Signed the FR-CHK-007 one-follow-on amendment. |
| 2026-09-07 | Claude (for William) | Built the FR-CHK-007 one-follow-on amendment: both receipts show "A newer session followed this one · open it" as a link when `restartedAs` is set, and no button. 313 web tests pass. |
| 2026-09-07 | Claude (for William) | FR-CHK-030 pause/resume on real sessions added; FR-CHK-005 reworded Cancel → Stop on the subscriber side; FR-CHK-018 gains Pause/Resume on account rows; parked pause item closed ([ADR 2026-09-07 subscriber pause](../decisions/2026-09-07-subscriber-pause-signed-relay.md)). Awaiting signature. |
| 2026-09-07 | William | Signed FR-CHK-030 and the FR-CHK-005/018 amendments. |
| 2026-09-07 | Claude (for William) | Built FR-CHK-030 and the FR-CHK-005/018 amendments: real checkout client relays pause/resume like cancel and polls to the new status; meter view says Stop, shows the paused copy and a frozen readout; cap step says "when you stop"; account client, mock and types gain `pause`/`resume` and `allowPause`; meter row shows a Pause/Resume strip under the row when the product allows it, "Paused · N left" while paused; running total reads "1 meter running · 1 paused". Checked at 390 px and 1280 px. 319 web tests green. |
| 2026-09-07 | Claude (for William) | FR-CHK-031 Add funds added; FR-CHK-003 and BR-CHK-001 amended ([ADR 2026-09-07 add funds](../decisions/2026-09-07-add-money-and-ausd-live-on-testnet.md)). Awaiting signature. |
| 2026-09-07 | William | Signed FR-CHK-031 and the FR-CHK-003/BR-CHK-001 amendments. |
| 2026-09-07 | Claude (for William) | Built FR-CHK-031 and the FR-CHK-003/BR-CHK-001 amendments: `AddMoneyStep` (balance, needed, QR via `qrcode` loaded on this step only, address + Copy, 5 s poll, "Funds arrived", Not now), `CapStep` gains `availableUsd` from FR-API-048 and an Add funds button when no preset is affordable, the page reads the balance once signed in and shows Add funds on the ready step or after a Start refused for funds; clients gain `getBalance` and the `insufficient_funds` code; seeded `/c/cs_short`. Checked at 390 px. 330 web tests green. |
| 2026-09-07 | William | Copy: "Add money" becomes "Add funds" everywhere on the step, the cap step button and the toast ("Funds arrived"). Applied. |
| 2026-09-09 | Claude (for William) | Judge pass: a stopped session reopened later rebuilt its receipt from `started_at → canceled_at`, counting paused time (105 s shown for 74 s billed), against BR-CHK-003. The real mapper now carries the server's `seconds_elapsed`/`settled_usd` on a canceled subscription and `buildReceipt` prefers them; the recount remains for the mock and the predicted cap end. |
| 2026-09-09 | William | Signed FR-CHK-032 (meter follows the server; 5 s poll and focus). |
| 2026-09-09 | Claude (for William) | Built FR-CHK-032: `checkout-page.tsx` re-reads the session every 5 s and on window focus while the subscription is active or paused, stops at the receipt; `checkout-page-follow.test.tsx` covers the external cancel, the server totals, the stop of polling, and focus. |
| 2026-09-11 | Claude (for William) | Live-mode run on the hosted app (`sub_uoI4vfJHJR255c`, real AUSD, 85 s, $0.34 settled, $14.06 refunded, 2% fee, six deliveries): passed. One defect recorded in Open — a merchant-fixed `max_duration_seconds` is dropped by the checkout's mapper, so the cap step rejects every choice with `cap_fixed`. William: document now, fix later. |
| 2026-09-13 | Claude (for William) | [ADR 2026-09-13 AUSD only](../decisions/2026-09-13-ausd-only-mockusd-to-test-fixture.md) applied: FR-CHK-003 test-mode mint exemption removed; FR-CHK-031 Add funds runs in both modes. **Awaiting signature.** |
| 2026-09-13 | William | Signed the AUSD-only amendments. |
| 2026-09-15 | Claude (for Furqaan) | **Merchant-started metering on the checkout and `/account`, awaiting sign-off** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). FR-CHK-033 returns the subscriber on its own once funding confirms, with a Back button after 20 s; FR-CHK-034 is the held view with the refund-by time and Stop; FR-CHK-035 adds a cap-step line for merchant-mode products; FR-CHK-036 lists held money on `/account`, outside the running total; FR-CHK-032 is amended to follow the held view. Found while grilling: `deriveView` maps every `incomplete` subscription to the Start step, so today a funded subscriber coming back is offered Start again. Depends on API FR-API-137/138. |
| 2026-09-16 | Furqaan | **Signed** FR-CHK-033–036 and the FR-CHK-032 amendment. |
| 2026-09-16 | Claude (for Furqaan) | **Built** FR-CHK-034 and the FR-CHK-032 amendment. `/impeccable` is not installed; Furqaan chose to build from existing components and DESIGN.md with no new direction. `mapSubscription` sets `hold` only for `incomplete` + `merchant` + a real deposit (`funded_usd`, never the cap); `deriveView` returns `held` before `used`, because merchant mode completes the session at funding. `HeldView` reuses the meter panel, primary Back link and neutral outline Stop; the page shares one Stop handler with the meter and follows the server while held. A stopped held meter gets its receipt on reload (the rebuild no longer bails on a null `startedAt`), and the receipt omits Started for a meter that never started. Mock seed `/c/cs_held`. Web: 391 tests. |
| 2026-09-16 | Claude (for Furqaan) | **Built** FR-CHK-033. Found and fixed on the way: the real client's `start` polled for `active`, which a merchant-mode meter never reaches, so a subscriber who authorised got "The network is taking longer than usual" after the timeout. In merchant mode `start` now returns after one re-read. The page records the authorisation, shows `AuthorisedView` in place of the Start step, re-reads every 2 s and leaves for `success_url?session_id=…` (via `leaveTo`, a one-function seam) once the session is `complete`; after 20 s it stops and shows Back. The authorised screen's second line, "You haven't been charged. Billing starts when {merchant} starts your session.", repeats FR-CHK-035's sentence. **Open:** the Start step still says "The meter starts the moment you press Start.", which is false in merchant mode and is not covered by the signed text. |
| 2026-09-16 | Claude (for Furqaan) | **Built** FR-CHK-035: `CapStep` takes `waitsForMerchant` and adds "Billing starts when {merchant} starts your session." under the existing line; the page passes it when `product.startMode` is `merchant` (from the public projection's `product.start_mode`, API revision 2026-09-16). Checkout-mode products are unchanged. |

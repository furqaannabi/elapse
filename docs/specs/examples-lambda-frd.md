# `examples/lambda` (per-second serverless compute) — FRD

Status: **FR-EXM-152 amendment (no cap step before the Elapse window) Signed 2026-09-20 (Furqaan)** · **FR-EXM-153/154 amendments (the session ends with the run) Signed 2026-09-20 (Furqaan)** · **FR-EXM-152 amendment (bundle the console with esbuild) Signed 2026-09-17 (Furqaan)** · **FR-EXM-152 and the FR-EXM-114 amendment (`@elapse/react`) Signed 2026-09-17 (Furqaan)** · **2026-09-15 merchant-start amendments (FR-EXM-102/114/115, new FR-EXM-125/126/133) Signed 2026-09-16 (Furqaan)** · **Signed 2026-09-12 (Furqaan)** · **2026-09-13 amendment (React/Monaco console) Signed 2026-09-13 (Furqaan)** · **2026-09-13 reversal to arbitrary JS execution awaiting Furqaan's sign-off** · Surface: Reference merchant (Node server + real AWS Lambda, terminal + console page) · Sources: detailed doc §4.2, §5.1–§5.3, §6, §7 step 5, §10; [examples-frd.md](./examples-frd.md) (the saas example this parallels); [ADR 2026-09-06 example merchant own brand](../decisions/2026-09-06-example-merchant-own-brand.md); [ADR 2026-09-04 subscriber permit](../decisions/2026-09-04-subscriber-permit-relayer-signs.md); [ADR 2026-09-05 keeper may cancel](../decisions/2026-09-05-keeper-may-cancel.md). Grilled 2026-09-12 (Furqaan); AWS runner infra provisioned live during the grill.

## Problem

`examples/saas` proves the smallest correct Elapse merchant, but its product is faked — "Acme GPU" gives you no GPU. Judges asking "can Elapse bill *real* usage of a *real* thing by the second?" want a second example where the metered product is genuinely running. Serverless compute is the sharpest demo of Elapse's core claim ("you only pay what elapsed"): a subscriber opens a live compute session, runs code on **real AWS Lambda**, and pays only the seconds the session was open — the session starting and ending on its own, and the merchant's server hearing about the end through a **webhook, not a cron job**.

This example deliberately trades the saas example's clone-and-run guarantee for realism: it requires an AWS account and a deployed runner (see Undecided 1). It is the "advanced" reference, not the first thing a judge runs.

## Non-negotiable framing (settled in the grill)

- **The subscription IS the session.** Elapse bills a running meter, never per invocation. The meter runs wall-clock from start to cancel; a subscriber pays for the seconds their session is **open**, regardless of how many times they invoke or how long each invocation takes.
- **Two clocks, never confused.** AWS bills the *merchant* per invocation-millisecond; Elapse bills the *subscriber* by the open session. The merchant's margin is `(session rate/s) − (AWS cost)`. The example prints both so the model is legible.
- **The live meter is an estimate; the billed amount is exact from settlement.** The console meter ticks client-side (`rate × elapsed`) as a preview. The amount the subscriber actually pays is the settled figure delivered in `subscription.canceled` (`seconds_elapsed`, `amount_settled`); the console shows that once the session ends.
- **No per-invocation webhooks, ever.** Lifecycle events only, exactly as the platform guarantees.
- **No chain words on the subscriber side.** The console and copy say "session", "seconds", "$". Never a token, address, or tx.
- **The session starts and ends automatically; there is no Start button and no Stop button.** The one irreducible gesture is the subscriber's permit authorization (Face ID) — consent to be charged, which cannot be removed. It is triggered inline by the subscriber's first Run. The session then ends on its own: on leaving, on inactivity, or at the escrow cap — with no action from the subscriber. Auto-end is server-initiated `subscriptions.cancel` (keeper-may-cancel ADR); the escrow cap is the hard on-chain backstop.

## User stories

1. As a judge, I want to just write code and hit Run and have the session begin by itself (one Face ID), so that I never think about "starting" a meter.
2. As a judge, I want the meter to keep ticking while I sit idle between runs, so that I understand I pay for the open session, not per invocation.
3. As a judge, I want the session to end by itself when I close the tab or walk away, so that I stop paying without doing anything.
4. As a judge, I want the server to learn the session ended via a webhook and refuse the next run, so that I see there is no cron job.
5. As a subscriber, I want to see the exact amount I was billed when the session ends, not just the ticking estimate, so that I trust the meter.
6. As a subscriber, I want a hard ceiling on what a session can ever cost me even if everything fails, so that a stuck session cannot drain me.
7. As a merchant engineer, I want to see how a metered live session maps onto Elapse (first run → checkout → active → auto-cancel → revoke), so that I can build my own metered-compute product.
8. As a merchant engineer, I want the smallest correct webhook handler (raw body, `constructEvent`, dedupe, 2xx fast), so that I can copy it, identical to the saas example.
9. As a security reviewer, I want the runner's isolation, its zero-value IAM role, and its residual risks stated plainly, so that I understand what is and is not safe.
10. As the account owner, I want a hard daily cap on executions, so that a runaway or abusive session cannot run up an AWS bill.
11. As a demo presenter, I want a branded console with a code box, a Run button, output, and a live meter (no Start/Stop buttons), so that the video shows "write code, it runs, walk away, it stops."
12. As the account owner, I want one documented path to provision and to tear down the AWS runner, so that I can reproduce or remove it.

## Functional requirements

### Setup and run

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-100 | `examples/lambda` is a standalone Node 20+ TypeScript project (run via `tsx`) depending on the **published** `@elapse/sdk` pinned `^0.1.0` plus `@aws-sdk/client-lambda` and `dotenv`; server on `node:http` with **no server framework and no bundler**. The browser console loads React 18.3.1 (UMD) and Monaco 0.52.2 as pinned `<script>` tags from a CDN — runtime browser assets, not npm dependencies (BR-EXM-104) — so `npm install && npm start` remains the only build step. React 19 is not used: it ships no UMD build, and an ESM import map is more moving parts than a demo console needs. It is **not** held to `examples/saas`'s zero-setup clone-and-run bar: it documents AWS prerequisites. | `npm install && npm start` succeeds with valid env; no bundler config exists in the package; missing AWS access fails with a readable message, not a stack trace. |
| FR-EXM-101 | `.env.example` lists `ELAPSE_SECRET_KEY`, `ELAPSE_WEBHOOK_SECRET`, `ELAPSE_API_URL`, `PORT=3000`, `BASE_URL=http://localhost:3000`, `AWS_REGION=us-east-1`, `LAMBDA_FN=elapse-lambda-runner`, `DAILY_RUN_LIMIT=20`, `MAX_DURATION_SECONDS=3600`, `IDLE_TIMEOUT_SECONDS=60`, `HEARTBEAT_STALE_SECONDS=15`, each with one comment on where it comes from. AWS credentials are **not** in `.env`; the standard AWS SDK credential chain is used. Missing `ELAPSE_SECRET_KEY`, `ELAPSE_API_URL`, or `LAMBDA_FN` exits 1 naming the variable. | Config loader unit test; exit code asserted per missing var. |
| FR-EXM-102 | `npm start` (a) creates or reuses a Product `"Serverless runtime"` at `"0.002"` USD/s (reuse by name via `products.list`), (b) prints `Product`, `Webhooks`, and `Runner: {LAMBDA_FN} @ {AWS_REGION}`, (c) listens on `PORT`. Checkout sessions are created per subscriber on demand at first Run (FR-EXM-114), not at boot. **Amended 2026-09-15 (awaiting sign-off, [ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)):** the Product is created with `startMode: "merchant"`, and an existing one is reused only when its name matches **and** its `start_mode` is `merchant`. A same-name checkout-mode product is skipped with one line: `Skipped "Serverless runtime" (prod_…): starts at checkout. Created prod_… in merchant mode. Archive the old one in the dashboard.` The example depends on `@elapse/sdk` `^0.1.4` (SDK FR-SDK-042). | Mock API + mock Lambda run asserts the product body (§4.2) and stdout snapshot. |
| FR-EXM-103 | The README has sections in this order: What this is · Prerequisites (incl. AWS) · Provision the runner · Run it · What you will see · How the session maps to Elapse · Security · Files · Teardown. Every non-AWS command is exercised by CI (FR-EXM-151). | README lint: headings snapshot; commands runnable. |

### Console (subscriber surface) — no Start/Stop buttons

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-110 | `GET /` serves the merchant's own-brand landing (one HTML file + shared CSS, no framework, per the own-brand ADR): name, "Serverless runtime", "$0.002 / second · ~$7.20 / hour", and an **Open console →** link (navigation only; it starts no billing). | Playwright: link points to `/console`. |
| FR-EXM-111 | `GET /console` serves a **React console** (React 18.3.1 UMD + Monaco 0.52.2, both from CDN, no build): a **Monaco editor holding the JavaScript to run** (default snippet: the Mandelbrot renderer), the runner's source shown read-only beside it, one **Run** button posting to `/run`, an output panel showing the rendered image and the Lambda's own duration, and the **live meter** ticking client-side from `rate × (now − started_at)` at 100ms, tabular numerals, no blinking. Before the first Run the meter reads `00:00 · $0.00` and is not charging. There is **no Start button and no Stop button**. It polls `/access` every 3s; on `active:false` it stops the meter and heartbeat, clears the session, and shows "Session ended — paid {n}s · ${amount}" from the settled figures, not the estimate. If the CDN is unreachable the page still renders and falls back to a plain textarea. No chain words. | Route test asserts the page references the pinned CDN scripts and carries the editor mount, Run, output and meter; idle/running/ended states checked headlessly (seam decision in Open). |
| FR-EXM-112 | `GET /cancel` shows "Checkout canceled. Nothing was charged." (the hosted-checkout cancel URL). | Route test. |
| FR-EXM-113 | `GET /access/:sub_id` returns `{ active, reason, seconds_elapsed?, paid_usd? }` JSON — the "is this session running, and if ended what did it cost" check. `paid_usd` is the subscriber-facing gross (`seconds_elapsed × rate`), not the merchant's net-of-fee `amount_settled`. | Unit test before/after a canceled Event; `paid_usd` computed as gross. |

### Automatic start (first Run)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-114 | On a `/run` with no active session for the viewer, the server does **not** execute; it creates a Checkout session (`successUrl = {BASE_URL}/console?session={cs}`, `cancelUrl = {BASE_URL}/cancel`) with `maxDurationSeconds = MAX_DURATION_SECONDS` and returns `409 { needs_start: true, checkout_url }`. The console stashes the pending code and redirects to `checkout_url` (the one Face ID / permit). On return to `/console?session={cs}`, the console resolves the `sub_id` (polls `/access` until active) and **auto-runs the stashed code**. The subscriber experiences one Run click plus Face ID; the meter starts at checkout completion. **Amended 2026-09-15 (awaiting sign-off, [ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)):** the checkout returns the subscriber by itself once funding confirms (checkout FR-CHK-033). The console polls `/access` until the session is `authorised` rather than `active`, then auto-runs the stashed code; that run is the first Run of FR-EXM-125 and starts the meter. No click is added. **Amended 2026-09-17 (awaiting sign-off, FR-EXM-152):** authorisation happens inline with `@elapse/react`, not by redirecting to a Checkout URL. | Integration with mock API: first run → 409 + checkout_url; after a simulated completed session, the stashed code runs; no second Run click needed. |
| FR-EXM-115 | The meter starts when the permit-backed stream starts (platform `start`, ADR 2026-09-04), never before. The console's client-side meter reads `started_at` from the active session so it matches the on-chain start. **Amended 2026-09-15 (awaiting sign-off, [ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)):** with merchant start, `started_at` is when the merchant's `start` confirmed on chain, carried by `subscription.updated` (FR-EXM-133), not the moment of authorisation. | Meter start time equals the session's `started_at`, not the page-load time. |
| FR-EXM-125 | **The first Run starts the meter** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). A `/run` for an `authorised` session calls `subscriptions.start(sub)`, marks the session `starting`, and waits for `subscription.updated` with `status: "active"` (FR-EXM-133) before invoking the runner, so every second of compute is on the meter. The console shows "Starting your meter…" on that first Run only. If `active` has not arrived 30 s after the start was accepted, the server cancels the session (a full refund, since nothing accrued) and returns `503 { error: "The meter didn't start, so nothing ran and nothing was charged." }`. A second `/run` while `starting` waits on the same start instead of calling `start` again. Later Runs invoke immediately. The daily cap (BR-EXM-109) is consumed only when the runner is actually invoked. | Mock SDK and clock: first Run → one `subscriptions.start` and no invoke until a signed `subscription.updated` active, then one invoke; no active within 30 s → one `subscriptions.cancel`, `503`, zero invokes, zero daily runs consumed; a concurrent second Run → still one `start`. |
| FR-EXM-126 | **Leaving before the meter starts** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). An `authorised` or `starting` session is cancelled at once, for a full refund, on the tab-close beacon (FR-EXM-118) or when its heartbeat is older than `HEARTBEAT_STALE_SECONDS` (FR-EXM-117). The idle timeout does not apply before start, because editing code costs nothing. The platform's unstarted sweep (worker FR-WRK-075) remains the backstop if the merchant server is down. | Store test: `authorised` + stale heartbeat → due `left`; `authorised` + no run for longer than `IDLE_TIMEOUT_SECONDS` → not due; beacon on an `authorised` session → one cancel. |

### Automatic end (leave / idle / cap)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-116 | While `/console` is open with an active session it POSTs `/heartbeat?sub={sub}` every `5s`; the server records `last_seen`. A `/run` also refreshes `last_seen` and `last_run`. | Heartbeat updates `last_seen`; asserted in a unit test. |
| FR-EXM-117 | A server sweep (every 5s) auto-ends any active session by calling `subscriptions.cancel` when either `now − last_seen > HEARTBEAT_STALE_SECONDS` (viewer left / tab closed / network gone) or `now − last_run > IDLE_TIMEOUT_SECONDS` (viewer idle with tab open). It logs `auto-ended (left)` or `auto-ended (idle)`. Because `cancel` is eventual (SDK: the `canceled` status arrives via webhook once the chain confirms), the sweep sets a `canceling` flag on the session after the call so it does **not** re-issue `cancel` on later ticks; the `subscription.canceled` webhook is what flips the session inactive (FR-EXM-131). | Unit tests with injected clock: stale `last_seen` → one `cancel` call with reason left; idle `last_run` → one `cancel` call with reason idle; a second sweep before the webhook does not re-call; active-and-recent → no cancel. |
| FR-EXM-118 | On `pagehide`/`beforeunload` the console fires `navigator.sendBeacon('/end?sub={sub}')`; the server cancels that session immediately (does not wait for the sweep), also setting `canceling`. | Beacon to `/end` → one `subscriptions.cancel` for that sub. |
| FR-EXM-119 | The escrow cap is the hard backstop: because first-Run checkout sets `maxDurationSeconds`, the permit caps `max_escrow = rate × MAX_DURATION_SECONDS`, and the meter ends itself on-chain at the cap (platform FR-CON-041, surfaced as `ended_reason: "cap_reached"`) even if the server and every auto-end path fail. The README states the resulting ceiling (default 1h → ~$7.20). No example code beyond passing `maxDurationSeconds`. | README states the cap ceiling; the checkout body carries `maxDurationSeconds`. |

### Runner / executor

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-120 | `POST /run?sub={sub_id}` with `{ code }`: if no active session → FR-EXM-114 (409 needs_start); else if the daily limit is reached → 429 (FR-EXM-140); else invoke the AWS runner and return `{ ok, result / error, ms, logs }`. The daily-limit and active checks run before any AWS call. | Tests: no session → 409; active + mock runner → 200; over limit → 429 with no runner call. |
| FR-EXM-121 | The executor is a **deep module**, one interface `run(code: string): Promise<RunResult>`, two implementations: `awsRunner` (real, `@aws-sdk/client-lambda` `InvokeCommand` on `LAMBDA_FN`) and `mockRunner` (deterministic, no network, tests/CI). The real runner is default; the mock is a test/CI seam only. | Unit tests on both against the same interface; `awsRunner` with the SDK client mocked. |
| FR-EXM-122 | **Reversed 2026-09-13 (Furqaan: "I want all js execution").** The runner Lambda executes **caller-supplied JavaScript**: it receives `{ code }`, runs it as an async function body inside the Lambda microVM, captures `console.log`, and returns `{ ok, result, ms, logs }` (or `{ ok:false, error, ms, logs }` on throw); `undefined` serialises as `null`. The Mandelbrot renderer is retained as the console's **default snippet**, not as the function's contract, so the demo still opens on a genuinely CPU-bound workload while any code may be run. **Amended 2026-09-21 (signed, Furqaan: "i want a default code that runs for 30s"):** the console opens on **thirty real seconds** of Lambda — the shipped default hashes in one-second slices, logging a line per slice, and returns `{seconds, hashes, digest}`. The previous default returned `"Hello, world!"` in about a millisecond, so the meter never moved and the thing the console exists to demonstrate was invisible. It burns CPU rather than sleeping deliberately: a subscriber asked to accept per-second billing should watch the seconds do work, not watch a clock run on an idle machine. `defaultSnippet(seconds)` takes the count so the suite runs one second instead of thirty; the console is always served `defaultSnippet()`. The Mandelbrot stays as the heavier alternative. **This required the runner's AWS timeout to go from 5 s to 60 s** (`aws lambda update-function-configuration --function-name elapse-lambda-runner --timeout 60`, done 2026-09-21) — at 5 s the default could only ever fail. Consequence Furqaan accepted: a Run now costs the subscriber about 30 s of meter (~$0.12 at $0.004/s), and `DAILY_RUN_LIMIT` is 20, so at most ~$2.40 a day. | Runner returns 4 for `return 2+2`, captures a logged line, reports a thrown error, and renders the shipped Mandelbrot snippet to a PNG data URI. |
| FR-EXM-123 | Invocation errors (throttle, timeout, function error, oversized response, invalid input) map to a readable `/run` response, never a raw AWS stack; a Lambda timeout surfaces as `{ ok:false, error:"execution timed out" }`. | Test with the SDK client mocked to throw each case. |
| FR-EXM-124 | `Provision the runner` documents the runner as deployed: IAM role `elapse-lambda-runner-role` trusting `lambda.amazonaws.com` with **only** `AWSLambdaBasicExecutionRole` (CloudWatch logs; no other AWS access), and function `elapse-lambda-runner` (`nodejs20.x`) in `us-east-1`, **redeployed with the renderer and raised to 1024MB / 10s** — Lambda's CPU scales with memory, so a CPU-bound render at the previous 128MB would crawl. A `Teardown` section documents deleting both. Lambda's native START/END/REPORT lines in CloudWatch Logs are where real invocations are visible — observability only, off the billing path. | README carries the exact CLI; a reviewer can reproduce and remove the infra. |

### Webhook handler and session gating

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-130 | `POST /webhooks` reads the **raw** body and calls `elapse.webhooks.constructEvent(rawBody, header, ELAPSE_WEBHOOK_SECRET)`; mirrors saas FR-EXM-020..025 (verify before act, 400 on bad signature, respond 2xx within 100ms before work, dedupe on `evt_` id). It keeps an in-memory session map `sub_ → { active, canceling, customer, started_at, last_seen, last_run, seconds_elapsed?, paid_usd?, updated_at }`. | Tests: raw-body pass, tampered/expired → 400, redelivery → no-op. |
| FR-EXM-131 | Lifecycle from §5.1: `checkout.session.completed` → log `provision session`; `subscription.created` → `active=true`, record `started_at`, log `session open sub_…`; `subscription.updated` → log `sync session (status)`; `subscription.canceled` → `active=false`, `canceling=false`, record `seconds_elapsed` and `paid_usd` (gross), log `session closed · {seconds_elapsed}s · ${paid_usd}`; `invoice.settled` → log `book revenue ${amount_settled}`; `invoice.payment_failed` → `active=false`, log `session closed (payment failed)`. Unknown → `ignored`. | One test per type asserts the map and the exact log line. |
| FR-EXM-132 | After `subscription.canceled` or `invoice.payment_failed`, the next `/run` for that `sub_id` returns 409 needs_start (a fresh session), and `/heartbeat`/`/end` for it are no-ops — the webhook, not a timer, is the source of truth for "closed". | Integration: run OK while active; signed canceled; run → 409 needs_start. |
| FR-EXM-133 | **Session states follow the webhooks** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). `subscription.created` for a `merchant`-mode subscription opens the session as `authorised`, not active, with no `started_at`; `subscription.updated` with `status: "active"` moves it to `active` with `started_at` from the event; `subscription.canceled` and `invoice.payment_failed` still close it (FR-EXM-132). Before start, `/access/:sub` returns `{ active: false, reason: "authorised" }` or `reason: "starting"`. | Webhook test: created → `authorised` with no `started_at`; updated active → `active` with the event's `started_at`; `/access` reason asserted in each state. |

### Rate limit (cost guard)

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-140 | The server enforces a hard `DAILY_RUN_LIMIT` **(default 20) executions per UTC day** across all sessions. A run beyond it returns `429 {"error":"daily execution limit reached"}` and is **not** sent to AWS. Resets at UTC midnight. Primary AWS-cost guard, and it matters more now the workload is CPU-bound: worst case 20 × 10s × 1024MB ≈ 205 GB-s per day (~6 GB-s-month equivalent of the 400,000 free tier), still comfortably inside it. | Unit test: 20 pass, 21st → 429 with no runner call; resets on a new UTC day (clock injected). README states the recomputed bound. |
| FR-EXM-141 | The runner's caps are defence in depth: 5s timeout and 128MB bound each invocation; the account's concurrency ceiling bounds fan-out. README states expected cost is within the AWS free tier. | README states the cost bound and the caps. |

### Demo readiness and CI

| Id | Requirement | Acceptance |
| --- | --- | --- |
| FR-EXM-150 | `npm run demo:check` sends a locally-signed `subscription.canceled` to `/webhooks` and exits 0 only if the server logs `session closed`, mirroring saas FR-EXM-030. | Script exit-code test. |
| FR-EXM-151 | CI runs the Elapse path against a local API and worker with the **mock runner** (no AWS, no chain): product created, a session opened via a delivered event, a `/run` served by the mock while active, an idle sweep (injected clock) or a signed `subscription.canceled` closing the session, and `/run` then returning needs_start — under 2 minutes. AWS is never called in CI. | GitHub Actions job green on relevant PRs and nightly. |
| FR-EXM-152 | **`@elapse/react` in the Lambda console** ([ADR 2026-09-17](../decisions/2026-09-17-react-sdk-replaces-hosted-checkout.md)). The console's first Run renders `<Authorize session>` inline (the popup handles Face ID), then `<Meter session>`; the merchant starts the meter as FR-EXM-125 describes, and ends it with `subscriptions.cancel`. The subscriber sees no Stop once started (FR-CHK-037). The browser build loads from `cdn.jsdelivr.net` beside React and Monaco. **Amended 2026-09-17 (signed, [ADR 2026-09-17 examples bundle](../decisions/2026-09-17-examples-bundle-react-sdk.md)):** the console (React, the Monaco React wrapper and `@elapse/react`) is bundled by `npm run build:web` with esbuild; the CDN UMD React is removed so the page runs one React. Monaco's editor workers may stay on the CDN. `npm start` builds first. **Amended 2026-09-20 (signed):** the console passes `cap={MAX_DURATION_SECONDS}` to `<Authorize>` (React FR-RCT-010 amended), so Run opens the Elapse window with no cap step in between. The server renders the cap into the mount node as `data-max-duration`; Northwind chooses it, as it already chooses the rate. **Amended 2026-09-21 (signed, Furqaan: "i want same meter design which is on elapse home page … i mean about lambda"):** the meter **undocks**. `dock="bottom-right"` is withdrawn, so `<Meter>` renders as the instrument card — placard head, readout, footer, the layout the meter on elapse.finance wears — in the slot `<Authorize>` occupies, at the top of the console. The editor and the output move down by the card's height; the capsule's reason for existing (staying out of the terminal's flow) is given up deliberately, because the meter is the thing being demonstrated. `controls={false}` and `proof` stand: Northwind's meter is Northwind's to stop (FR-CHK-037), and no pause control appears here at all — under FR-EXM-153 the session ends with each run, so there is nothing to pause. The console upgrades to `@elapse/react@0.4.1`. | Console test with a mocked popup: Run → inline authorise → held → active → meter; no navigation to a hosted page. **Amended:** `GET /console` carries `data-max-duration`, and the console renders no cap step. |
| FR-EXM-153 | **The meter runs only while code runs** (signed 2026-09-19, [ADR](../decisions/2026-09-19-keeper-may-pause.md)). After the first Run starts the meter (FR-EXM-125), the server pauses it as soon as that run returns, and resumes it immediately before each later run, including when the run throws — the `finally` path pauses too. `subscriptions.pause`/`resume` (SDK FR-SDK-044) are the calls; `paused` and `active` arrive by webhook (FR-EXM-133) and the console's `<Meter>` follows the server. A run therefore bills its own duration plus the confirmation time of its resume and pause, about 1–3 s, not milliseconds — the console says so beneath the meter: "You pay for the seconds your code runs." A resume that does not confirm within 30 s fails the run the way FR-EXM-125 fails a start, and the session is left paused rather than accruing. **Amended 2026-09-19 (awaiting sign-off, Furqaan chose "end it immediately"):** the meter is not paused between runs — the session **ends** when the run does. `/run` starts the meter, invokes, then cancels through `subscriptions.cancel`, and the receipt appears at once. One session per execution: the next Run opens a new checkout session and asks for a fresh authorisation, which is a second Face ID for a subscriber whose device already holds a passkey and a full sign-in for one who does not. `subscriptions.pause`/`resume` are no longer used by this example; they remain in the API and the SDK for merchants who want a session that stays warm. | Mock SDK and clock: one run → one resume, one invoke, one pause, in that order; a failing run still pauses; two runs → two resume/pause pairs and no second start; a resume that never confirms → `503`, no invoke, still paused. **Amended:** one run → one `start`, one invoke, one `cancel`, in that order, and no `pause`/`resume`; the receipt appears when the cancel confirms; a second Run opens a new session instead of resuming the old one. |
| FR-EXM-154 | **The idle timeout stops being a billing control** (signed 2026-09-19, same ADR). With FR-EXM-153 a paused session costs the subscriber nothing, so `IDLE_TIMEOUT_SECONDS` only reclaims abandoned sessions: it ends a session that has been idle that long, and the tab-close beacon and stale-heartbeat sweep (FR-EXM-117/118/126) are unchanged. The README stops describing idle time as billed. **Amended 2026-09-19 (awaiting sign-off, same decision):** with a session that ends at the end of its run, the idle timeout no longer governs a running meter at all. It survives as cleanup for a session that was authorised and never started (FR-EXM-126) and for one whose cancel never confirmed. | Store test: an idle paused session is due for auto-end; the receipt for a session that ran 2 s and idled 5 min shows the seconds it actually ran. **Amended:** a session that ran is already ended, so nothing idles; the idle path is asserted only for a session authorised and never started, and for one whose cancel never confirmed. |

## Business rules

| Id | Rule |
| --- | --- |
| BR-EXM-100 | Every Elapse call goes through `@elapse/sdk`, frozen methods only (BR-SDK-001); no hand-written `fetch` to the platform. |
| BR-EXM-101 | Always verify before acting; no code path touches the session map without a successfully constructed Event. |
| BR-EXM-102 | Respond 2xx first, work second; the handler never awaits merchant logic before responding. |
| BR-EXM-103 | Idempotent on `evt_` id; a redelivered Event is a no-op. |
| BR-EXM-104 | npm dependencies stay capped at `@elapse/sdk`, `tsx`, `dotenv`, `@aws-sdk/client-lambda` — no server framework, no bundler. **Amended 2026-09-13:** the browser page may additionally load **pinned CDN assets** (React 18.3.1 UMD, Monaco 0.52.2). These are runtime browser assets rather than dependencies, they add no install or build step, and the page must still render without them. |
| BR-EXM-105 | AWS credentials come only from the standard SDK chain, never from `.env` and never logged. The runner's IAM role carries logs-only access and nothing else. |
| BR-EXM-106 | Money and rates are decimal strings; the subscriber receipt is `seconds_elapsed × rate` (gross paid), computed with decimal-safe math, never `parseFloat`. |
| BR-EXM-107 | No chain vocabulary in the landing, console, or logs. Subscriber copy is "session", "seconds", "$". |
| BR-EXM-108 | **Reversed 2026-09-13:** the example executes caller-supplied code **only inside the AWS Lambda runner**, never in the server process. The isolation boundary is Lambda's per-invocation microVM plus a zero-permission execution role, a clamped timeout and the daily cap. **Residual risk, accepted by Furqaan:** a Lambda outside a VPC retains outbound internet, so submitted code can reach the network; this is a demo runner, not a hardened sandbox for hostile users. |
| BR-EXM-109 | The daily execution limit is checked before any AWS call, so the cost guard cannot be bypassed by an invocation error path. |
| BR-EXM-110 | Auto-end is always server-initiated `subscriptions.cancel` (keeper-may-cancel ADR), issued at most once **while one is in flight** (the `canceling` guard). **Amended 2026-09-13, awaiting Furqaan's review:** the original "at most once per session" wording stranded a session whose cancel *failed* — it stayed flagged and was never swept again, so it kept accruing to the cap. A failed cancel now clears the guard so the next sweep retries, bounded to 5 attempts, after which the sweep gives up and the escrow cap is the backstop. The escrow cap (`maxDurationSeconds`) is the on-chain backstop; the client can hasten end (beacon) but is never trusted to be the only thing that ends a session. |

## Interfaces

```
examples/lambda/
  README.md              FR-EXM-103 (incl. Provision, Security, Teardown)
  .env.example           FR-EXM-101
  package.json           scripts: start, demo:check, test, typecheck
  src/index.ts           boot: product, http server; routes / , /console, /cancel, /access/:id, /run, /heartbeat, /end, /webhooks
  src/executor.ts        deep module: run(input) -> RunResult; awsRunner + mockRunner (FR-EXM-121)
  src/session.ts         session map + evt_ dedupe + daily counter + idle/stale sweep (FR-EXM-116/117/130/140)
  src/webhooks.ts        // region:verify … // region:handle
  runner/index.mjs       the deployed AWS Lambda renderer (FR-EXM-122) — zero-dependency PNG via node:zlib
  public/{index,console,cancel}.html + brand CSS   FR-EXM-110/111/112 (console = React + Monaco from CDN)

RunResult = { ok: true, result: unknown, ms: number, logs: string[] }    // a data:image string renders as an <img>
          | { ok: false, error: string, ms: number, logs: string[] }
```

Live AWS runner (provisioned 2026-09-12, account 598046560354, `us-east-1`): function `elapse-lambda-runner` (`arn:aws:lambda:us-east-1:598046560354:function:elapse-lambda-runner`), role `elapse-lambda-runner-role` (AWSLambdaBasicExecutionRole only), 5s / 128MB.

Example terminal:

```
$ npm start
Product:  prod_…  Serverless runtime  $0.002/s
Webhooks: POST http://localhost:3000/webhooks
Runner:   elapse-lambda-runner @ us-east-1

14:02:11  evt_…  subscription.created   → session open sub_…
14:02:19  ▶ run sub_…  return 2+2  → 4  (9ms)   [1/20 today]
14:03:20  ⏹ auto-ended (idle) sub_…
14:03:21  evt_…  subscription.canceled  → session closed · 62s · $0.12
14:03:25  ▶ run sub_…  → 409 needs_start
```

## Undecided (human) — all resolved at sign-off 2026-09-12

1. ~~**AWS-only positioning.**~~ **Accepted.** This example requires AWS and is a deliberate exception to the saas clone-and-run bar, recorded in [ADR 2026-09-12 examples/lambda AWS-only](../decisions/2026-09-12-examples-lambda-aws-only.md).
2. ~~**Product name / rate.**~~ **Accepted:** `"Serverless runtime"` at `"0.002"` USD/s.
3. ~~**Auto-lifecycle windows.**~~ **Accepted:** first-Run start; auto-end via heartbeat 5s + stale reap 15s + idle-since-run 60s + beacon on close; escrow cap 3600s (~$7.20) as backstop.
4. ~~**Executor selection.**~~ **Accepted:** real `awsRunner` in production; `mockRunner` is a test/CI seam only.
5. ~~**Receipt figure.**~~ **Accepted:** subscriber sees gross paid (`seconds_elapsed × rate`), not the net-of-fee amount.

## Open

- **Reserved concurrency could not be set to 1**: this AWS account's total concurrency limit is 10, and any reservation drops unreserved below AWS's minimum of 10. The daily-run limit (FR-EXM-140) is the primary cost guard; 5s/128MB and the account ceiling are defence in depth. Revisit if the limit is raised.
- **CloudWatch is off the money path by decision.** It was considered for auto-end and rejected: it cannot reach `localhost`, alarms lag minutes (overcharging the tail), and would add an inbound public endpoint. Auto-end is the server sweep + beacon; the cap is the guarantee. CloudWatch remains only as native invocation logs for the demo.
- Publish the example as a standalone GitHub template repo, as noted for saas.
- Live-streaming Lambda logs to the console is out of scope for v1 (single request/response).

## Revision

| Date | Who | Change |
| --- | --- | --- |
| 2026-09-12 | Claude (for Furqaan) | First draft from the 2026-09-12 grill: AWS-only per-second serverless-compute example; live compute session billed wall-clock; runner executes subscriber-submitted JS on real AWS Lambda (infra provisioned live: `elapse-lambda-runner`, logs-only role, 5s/128MB, us-east-1); saas-style entitlement/webhook gating; 20/UTC-day run limit as the cost guard; `node:http`; deps = saas set + `@aws-sdk/client-lambda`. |
| 2026-09-12 | Claude (for Furqaan) | Auto-lifecycle + billing clarity from the grill: no Start/Stop buttons; first Run starts the session (one inline Face ID, FR-EXM-114/115); auto-end via heartbeat + stale reap + idle timeout + tab-close beacon with server-initiated `subscriptions.cancel` guarded by a `canceling` flag (FR-EXM-116..118, BR-EXM-110); escrow cap as on-chain backstop (FR-EXM-119); the console shows the exact settled receipt (gross paid), not the estimate (framing, FR-EXM-111/113/131). CloudWatch rejected as the end trigger (Open). SDK `subscriptions.cancel` verified against the frozen source. Awaiting Furqaan's sign-off. |
| 2026-09-12 | Furqaan | **Signed.** All five Undecided items accepted at the recommended defaults; AWS-only exception recorded as [ADR 2026-09-12](../decisions/2026-09-12-examples-lambda-aws-only.md). Build may start. |
| 2026-09-13 | Claude (for Furqaan) | Built: FR-EXM-100/101 (scaffold, config), 102 (boot; Product created, no Checkout session at boot), 113/114 (access, first-Run 409 `needs_start`), 116/117/118 (heartbeat, auto-end sweep, tab-close beacon, single-cancel `canceling` guard), 119 (cap passed as `maxDurationSeconds`), 120–123 (run route, executor deep module, runner error mapping), 130–132 (webhooks, lifecycle, webhook closes the session), 140 (daily cap), 150 (`demo:check`). 32 tests green, typecheck clean. **Two deviations from the text, for review:** (1) the example splits into `src/boot.ts` + `src/server.ts` rather than one `src/index.ts`, as `examples/saas` does; (2) FR-EXM-114 specifies `successUrl = {BASE_URL}/console?session={cs}`, but the build passes `{BASE_URL}/console` and lets the platform append the session id, matching how saas uses `/ok` — the FR text may want amending. **Not built:** FR-EXM-110/111/112 (landing, console, cancel pages) and 103/124/141 (README) — the pages are blocked because CLAUDE.md requires `/impeccable` before visual work and that skill is not installed in this repo, and the own-brand ADR means this example needs its own `DESIGN.md`. FR-EXM-151 is partial: the lambda unit suite runs in CI's `packages` job with the runner mocked; the local-API integration run is still to write. |
| 2026-09-13 | Claude (for Furqaan) | Surface completed: FR-EXM-110/111/112 (landing, console, cancel) on this example's own brand, recorded in `examples/lambda/DESIGN.md` — a field-terminal world, deliberately outside the root `DESIGN.md` per the own-brand ADR. **CLAUDE.md requires `/impeccable` before visual work; that skill is not installed in this repo, so the mandated design step did not run** and the direction was chosen directly. FR-EXM-103/124/141 (README incl. the exact provisioning CLI, security caveats, cost guards and teardown). Also fixed a defect found while building the console: the first-Run 409 carries no subscription id (none exists until checkout completes), so resume could never work — `checkout.session.completed` now records the `cs_ → sub_` link, `GET /session/:cs` exposes it, and the console resumes from `session_id` on the success URL. `/access` gained `started_at` because FR-EXM-115 requires the meter to start from the session, not page load. 37 tests green, typecheck clean. **Still open:** FR-EXM-151's local-API integration run (the lambda unit suite runs in CI's `packages` job with the runner mocked; the end-to-end script is unwritten and deliberately not committed unverified). |
| 2026-09-13 | Claude (for Furqaan) | FR-EXM-151 completed as far as it can be here: `examples/lambda/ci/integration.sh` plus a `lambda-example` workflow job exercise the Elapse half against a local platform with the runner mocked — seed a key, install a copy of the example from npm outside the workspace, assert `npm start` names the Product and the runner and opens **no** checkout session, then a delivered `subscription.created` opens the session, `/run` executes on the mock, a delivered `subscription.canceled` closes it, the next `/run` answers `409 needs_start`, and `demo:check` passes. AWS is never called and no chain is touched. **Written but not executed** (this session had no Postgres/API/worker running, and Furqaan chose to let CI prove it): the first green run of the `lambda-example` job is its verification. Supersedes the "unwritten" note in the row above. |
| 2026-09-13 | Claude (for Furqaan) | Fixed two defects found by running the example live against the hosted API. (1) A checkout-create failure (the account had no payout address) surfaced to the console as an opaque `500 Something went wrong.`; `/run` now returns `502 {error}` carrying the platform's own sentence (FR-EXM-100's "readable message"). (2) A failed `subscriptions.cancel` left the session flagged `canceling` forever, so it was never retried and kept accruing until the escrow cap — and the rejection propagating out of `sweepOnce` aborted the rest of that tick, skipping other due sessions. Cancel failures are now caught, retried up to 5 sweeps, then given up on. **This amends signed BR-EXM-110** (see its row) and needs Furqaan's review. 39 tests green, typecheck clean. Verified live: platform → CLI listener → example opened a session, real AWS Lambda returned a result in 319ms, and the sweep auto-ended the idle session exactly once. |
| 2026-09-13 | Claude (for Furqaan) | **FR-EXM-111's ended state was never implemented** — found by running the real flow, not by the suite. The server ended the session correctly (auto-ended idle at 82s, settled 90s/$0.18 with an on-chain tx), but the console fetched `/access` exactly once in `adopt()` and never again, so the meter ticked forever, the page still read "Running", the heartbeat kept firing, and the settled receipt never appeared. Fixed: the console now polls `/access` every 3s while a session is adopted, and on `active:false` stops the meter and heartbeat, clears `sub` (so the beacon stops and the next Run opens a fresh session) and renders "Session ended — paid {n}s · ${amount}" from the settled figures; `adopt()` also renders the ended state when it lands on an already-closed session. Reproduced and verified with a throwaway headless harness driving the real `console.html` against a stub `/access`. **Spec contradiction for Furqaan:** FR-EXM-110/111's acceptance says "Playwright: idle, running, ended states", but BR-EXM-104 caps this example's dependencies and excludes Playwright. Because of that there is **no correct permanent regression seam** for client behaviour, which is exactly how a missing feature sat behind 39 green tests. Needs a decision: amend BR-EXM-104 to allow playwright as a devDependency, or amend the FR acceptance to drop Playwright and accept the gap. |
| 2026-09-13 | Claude (for Furqaan) | **Amendment, awaiting sign-off.** Furqaan chose (a) replacing the eval-runner with a real workload and (b) a React + Monaco console loaded from CDN with no bundler. Rewrites FR-EXM-122 (the Lambda renders a Mandelbrot tile from clamped `{width,height,iterations}` and returns a PNG encoded with `node:zlib`; it never executes caller-supplied code), FR-EXM-120/121 (`/run` carries `{ input }`; executor takes `RunInput`), FR-EXM-111 (React 18.3.1 UMD + Monaco 0.52.2 from CDN — React 19 ships no UMD build; Monaco edits the JSON request, the runner's source sits read-only beside it), FR-EXM-100 and BR-EXM-104 (CDN assets permitted, npm deps unchanged, still no bundler), FR-EXM-124 (runner redeployed at **1024MB / 10s** because Lambda CPU scales with memory) and FR-EXM-140 (cost bound recomputed). BR-EXM-108 becomes **stronger**: no caller-supplied code runs anywhere, which removes the central risk the AWS-only ADR and the README Security section were written around — that ADR is a dated record and is not edited; a superseding ADR is offered. Requires a real redeploy of `elapse-lambda-runner` in Furqaan's account. |
| 2026-09-13 | Furqaan | **Signed** the real-workload + React/Monaco amendment after review. Build may start. The AWS redeploy of `elapse-lambda-runner` (1024MB / 10s, renderer code) is a separate go, not covered by this signature. |
| 2026-09-13 | Claude (for Furqaan) | **Amendment built.** FR-EXM-122: the runner is now a real workload — it renders a Mandelbrot tile from clamped `{width,height,iterations}` and returns a PNG encoded with `node:zlib` only (zero deps), and ignores any `code` field. FR-EXM-120/121: `/run` carries `{ input }`, validated before a daily run is spent; the executor takes `RunInput` and returns a tile; `mockRunner` is deterministic. FR-EXM-111: the console is React 18.3.1 + Monaco 0.52.2 from a pinned CDN with no bundler, editing the JSON request, with the runner's source served read-only at the new `/runner-source` route and the rendered tile as output; it keeps the meter, heartbeat, beacon and settled-receipt behaviour, and falls back to a textarea if the CDN is unreachable. FR-EXM-103/124/140/141: README rewritten — provisioning at **1024MB / 10s** (with an update-in-place path for the existing function), cost bound recomputed, and the Security section now leads with the fact that **no caller-supplied code executes anywhere**. `runner/index.d.mts` added so the tests typecheck against the deployed contract. 43 tests green, typecheck clean. Verified headlessly against the real `console.html`: React and Monaco mount from the live CDN, Run posts `{"input":{…}}`, the tile renders, and the page shows "Session ended — paid 90s · $0.18" with the meter frozen when the session closes. **Not yet done:** the AWS redeploy of `elapse-lambda-runner` (still the old eval-runner at 128MB/5s in Furqaan's account) — held pending his go. |
| 2026-09-13 | Claude (for Furqaan) | **Reversal, awaiting sign-off.** Furqaan: "I want all js execution". The runner goes back to executing caller-supplied JavaScript (FR-EXM-122), `/run` carries `{ code }` again (FR-EXM-120/121), and the Monaco editor holds code rather than a JSON request (FR-EXM-111). The Mandelbrot renderer is kept as the editor's **default snippet**, so the demo still opens on a CPU-bound workload that makes per-second billing legible, but any code may be run; the console renders an `<img>` when the returned value is a `data:image` string. **BR-EXM-108 is reversed and the security posture regresses:** arbitrary code executes in Furqaan's AWS account again, bounded by the zero-permission role, the clamped timeout and the 20/day cap — with the residual risk that a Lambda outside a VPC keeps outbound internet. Flagged explicitly and accepted by Furqaan; the README Security section and the AWS-only ADR rationale revert with it. This supersedes the real-workload half of the amendment signed earlier today. |
| 2026-09-13 | Claude (for Furqaan) | **Reversal built.** The runner executes submitted JavaScript again; `/run` and the executor carry `{ code }`; Monaco holds JavaScript with the Mandelbrot served from `/default-snippet` as its opening value, and a `data:image` result renders as an image while anything else prints as a value. Two things found while building, both worth recording: submitted code had **no module access at all** (`require` was `undefined`), so `require` is now passed into the sandbox — and dynamic `import()` inside `new Function` is **host-dependent** (it fails with "A dynamic import callback was not specified" under vitest while working in plain Node), so nothing shipped relies on it. Confirmed working: `fetch` reaches the network (status 200), sorting/async/`require('node:crypto')` all run, and the snippet renders a real PNG in 61ms. README Security rewritten — **outbound network access is now documented as a wanted capability, not a tolerated risk**, with the logs-only IAM role named as the actual boundary. 48 tests green, typecheck clean, console verified headlessly end to end. Still not done: the AWS redeploy. |
| 2026-09-13 | Claude (for Furqaan) | **Correction + measured state of the deployed runner.** An earlier note in this table and in conversation implied the deployed `elapse-lambda-runner` expected `{width,height,iterations}` and so could not serve the console. That was wrong: the Mandelbrot renderer was only ever local and was never deployed (the redeploy was correctly held), so the deployed function has always been the original eval-runner and today's reversal brings the repo back into alignment with it. Probed directly 2026-09-13 against the live function: `return 2+2` → 4, `typeof fetch` → function, a real `fetch` to the platform → 200 in 2457ms, sorting → [1,2,3]. **The one real gap: `typeof require` → "undefined"**, so the editor's default snippet fails live with `require is not defined`. The redeploy is therefore not cosmetic — it is what makes the console's opening snippet work — and it also moves the function from 128MB/5s to 1024MB/10s. Still awaiting Furqaan's go; the update-in-place commands are in the README. |
| 2026-09-13 | Claude (for Furqaan) | **Default snippet is now hello-world** (Furqaan: "default code console hello world"), with the Mandelbrot kept as `MANDELBROT_SNIPPET` because it is the thing that demonstrates per-second billing. This has a useful side effect: the default uses only `console.log` and `return`, so **the console now works against the already-deployed runner with no redeploy** — probed live: `{"ok":true,"result":"Hello, world!","logs":["Hello from Lambda"]}`. The redeploy is therefore **no longer blocking**; it is needed only for `require` (so the Mandelbrot example runs) and for the 1024MB/10s headroom. Also replaced the regex in `server.ts` that parsed `DEFAULT_SNIPPET` out of its own source with a real import — adding a second export would have silently broken it. 49 tests green, typecheck clean. |
| 2026-09-15 | Claude (for Furqaan) | **Merchant-started metering in the Lambda example, awaiting sign-off** ([ADR 2026-09-15](../decisions/2026-09-15-merchant-started-checkout-and-lambda.md)). The meter starts on the first Run, which on return from checkout is the stashed code running by itself (FR-EXM-114, FR-EXM-125); the runner is invoked only once `subscription.updated` says active, with a 30 s timeout that cancels and refunds; leaving before start cancels at once, with no idle timer (FR-EXM-126); `subscription.created` now means authorised, not running (FR-EXM-133); boot reuses the product only when it is merchant-mode (FR-EXM-102). Found while grilling: the example resolves `@elapse/sdk` 0.1.0 from npm, which has no `subscriptions.start`, so it depends on SDK FR-SDK-042 being published. |
| 2026-09-16 | Furqaan | **Signed** the 2026-09-15 merchant-start amendments (FR-EXM-102/114/115, FR-EXM-125/126/133). |
| 2026-09-17 | Claude (for Furqaan) | **FR-EXM-152, awaiting sign-off** ([ADR](../decisions/2026-09-17-react-sdk-replaces-hosted-checkout.md)): the console authorises and meters inline with `@elapse/react`. |
| 2026-09-17 | Furqaan | **Signed** FR-EXM-152 and the FR-EXM-114 amendment (`@elapse/react`). |
| 2026-09-17 | Claude (for Furqaan) | **FR-EXM-152 amendment, awaiting sign-off** ([ADR](../decisions/2026-09-17-examples-bundle-react-sdk.md)): the console is bundled with esbuild by `build:web`; UMD React from the CDN is removed. |
| 2026-09-17 | Furqaan | **Signed** the FR-EXM-152 amendment (esbuild bundle). |
| 2026-09-18 | Claude (for Furqaan) | **Built** the signed merchant-start and `@elapse/react` work: session states from the webhooks (FR-EXM-133), the first Run starts the meter and waits for the chain with a 30 s refund timeout (FR-EXM-125), leaving before start cancels with no idle timer (FR-EXM-126), the Product is created `merchant`-mode and a checkout-mode one of the same name is not reused (FR-EXM-102), `/run` answers `409 { needs_start, session }` with no checkout URL (FR-EXM-114), and the console is a bundled React island rendering `<Authorize>` then `<Meter>` with the UMD React gone (FR-EXM-152). 70 tests green. |
| 2026-09-19 | Claude (for Furqaan) | **FR-EXM-153/154, awaiting sign-off** ([ADR 2026-09-19](../decisions/2026-09-19-keeper-may-pause.md)). Live run showed a 0 ms invocation billing 62 s · $0.12, all of it the idle tail; the meter will run only while code runs. Blocked on the contract redeploy, API FR-API-141/142 and SDK 0.3.0. |
| 2026-09-19 | Furqaan | **Signed** FR-EXM-153/154 (the meter runs only while code runs). |
| 2026-09-19 | Claude (for Furqaan) | **FR-EXM-153/154 amendments, awaiting sign-off.** Furqaan, watching a session sit for a minute after its run: "no idle stop after execution". The session now ends with the run. Stated cost: a Face ID per execution, and the pause/resume machinery built earlier today goes unused *here* — it stays in the API, the SDK and the contract for merchants whose resource stays warm between calls. |
| 2026-09-20 | Furqaan | **Signed** the FR-EXM-153/154 amendments: the session ends with its run, at the cost of an authorisation per execution. |
| 2026-09-20 | Claude (for Furqaan) | **Built** the signed FR-EXM-153/154 amendments: `/run` starts, invokes and cancels (the `finally` path ends the session), a session whose cancel is in flight needs a new one, the daily-cap refusal ends the meter it just started, and `pause`/`resume` leave this example (deps, boot wiring, the `pausing`/`resuming` states and their tests). The idle path is now cleanup for a cancel that never confirmed. 85 tests green; `build:web` clean. |
| 2026-09-20 | Claude (for Furqaan) | **FR-EXM-152 amendment, built, awaiting sign-off.** The console passes `cap={MAX_DURATION_SECONDS}` so Run goes straight to the Elapse window (React FR-RCT-010 amended); the page carries `data-max-duration`. The stale "the meter is paused between runs" note in `console.html` is gone with it. 85 tests green. |
| 2026-09-20 | Furqaan | **Signed** the FR-EXM-152 amendment: Run opens the Elapse window with no cap step, Northwind's `MAX_DURATION_SECONDS` being the cap. |
| 2026-09-21 | Claude (for Furqaan) | **FR-EXM-152 amendment, awaiting sign-off:** the console's meter undocks and wears the homepage instrument. Found while chasing "no ui change showing in example": the console passes `dock="bottom-right"`, which renders the capsule, not the instrument — the same cause as in `examples/saas`. No pause control is added: FR-EXM-153 ends the session with each run. |
| 2026-09-21 | Claude (for Furqaan) | **FR-EXM-122 amendment, built, awaiting sign-off:** the default snippet runs thirty real seconds. Proven locally end to end — 30.0 s wall clock, 30 log lines, 121 M hashes. The runner's AWS timeout was raised 5 s → 60 s in the same change, since no 30 s default can run under a 5 s timeout. |
| 2026-09-21 | Furqaan | **Signed** the FR-EXM-152 amendment (the console's meter undocks and wears the homepage instrument). FR-EXM-122's thirty-second default is still awaiting signature. |
| 2026-09-21 | Furqaan | **Signed** the FR-EXM-122 amendment: the console opens on thirty real seconds, and the runner's AWS timeout stands at 60 s. |

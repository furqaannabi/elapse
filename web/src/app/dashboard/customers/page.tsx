import { DetailPlaceholder } from "@/components/dashboard/split-layout";

export default function DashboardCustomers() {
  return <DetailPlaceholder>Select a customer to see their meters and events.</DetailPlaceholder>;
}

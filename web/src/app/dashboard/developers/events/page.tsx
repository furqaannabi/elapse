import { DetailPlaceholder } from "@/components/dashboard/split-layout";

export default function DeveloperEvents() {
  return <DetailPlaceholder>Select an event to see its payload and deliveries.</DetailPlaceholder>;
}

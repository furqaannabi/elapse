import { DetailPlaceholder } from "@/components/dashboard/split-layout";

export default function DashboardSubscriptions() {
  return <DetailPlaceholder>Select a meter to see it live, with its timeline and settlements.</DetailPlaceholder>;
}
